- [x] 分镜生成接口在没有最新成功剧本时返回依赖缺失错误，不创建新的分镜文本产物，也不替换已有镜头列表。
- [x] 分镜生成使用最新成功剧本内容和项目 brief。
- [x] Seed 文本分镜 prompt 或等价请求包含剧本正文、商品名称、广告需求、投放平台、画面比例、视频时长、视觉风格和目标受众。
- [x] 生成分镜文本包含镜头编号、镜头时长、画面描述、主体/场景、运镜、旁白或字幕、音效和转场建议。
- [x] 结构化 `StoryboardShot` 列表按镜头编号从 1 连续递增。
- [x] 每个结构化镜头的 `duration_seconds` 大于 0。
- [x] 全部结构化镜头的 `duration_seconds` 总和与 brief `duration_seconds` 在明确容差内匹配。
- [x] 分镜结果以 `storyboard` 阶段文本产物写入 MySQL，并通过项目详情 API 返回。
- [x] 分镜镜头列表写入 MySQL，并通过项目详情 API 返回。
- [x] 分镜任务状态正确更新为 running、succeeded 或 failed，并支持失败重试。
- [x] 分镜生成失败时 API 和前端错误提示不暴露密钥、堆栈或供应商原始敏感错误。
- [x] 上游剧本变更后，下游分镜、图片、视频和成片按现有规则标记 stale。
- [x] 工作台能触发分镜生成、展示任务状态，并在成功后刷新项目详情。
- [x] 工作台能展示最新成功分镜文本的标题、版本、更新时间和正文。
- [x] 工作台能展示结构化分镜镜头列表，包含镜头编号、时长、画面描述、旁白或字幕、视觉提示词和状态。
- [x] 后端测试覆盖剧本缺失、成功生成、brief 字段进入分镜、总时长匹配、失败重试和 stale 标记。
- [x] 前端测试覆盖分镜生成触发、任务状态、成功刷新、分镜文本展示和镜头列表展示。
- [x] `.venv` 后端测试通过，前端 lint、typecheck、test、build 通过。
- [x] 本地 smoke test 能完成“剧本已生成 -> 触发分镜脚本生成 -> 查看分镜文本和镜头列表 -> 校验总时长”。
