- [ ] 节点面板只提供一个 Seedream 图片模型节点，不存在重复的独立图层拆分模型节点
- [ ] 旧 `image_to_image` 节点缺少 `operation` 时按图生图模式正常加载、保存和运行
- [ ] 图生图模式接受 1 至 10 个 `image_asset` 和必填提示词，仅输出 `image_asset`
- [ ] 普通图片编辑模式只接受一个 `edit_image` 和必填提示词，仅输出 `image_asset`
- [ ] 指定图层编辑模式只接受一个 `edit_layer` 和必填提示词，仅输出 `edited_layer`
- [ ] 图片编辑模式严格要求 `edit_image` 与 `edit_layer` 二选一
- [ ] 图层拆分模式只接受一张图片、允许空提示词，并仅输出 `layer_set`
- [ ] 模式切换保留不兼容连线、显示可定位错误并禁止运行，不静默删除连线
- [ ] 图层拆分请求使用 Seedream 5.0 Pro 且发送 `layer_decomposition=true`
- [ ] 图层拆分输入格式、比例、总像素和文件大小在前后端按官方约束校验
- [ ] 拆分结果包含唯一底图和 1 至 16 个连续层级的透明 PNG 图层
- [ ] 任一图层失败或响应非法时整项任务失败，部分内部资产和关联得到清理
- [ ] 底图、拆分图层、编辑图层和最终图片均记录完整的任务资产关联
- [ ] 内部底图和透明图层不会出现在普通资产选择列表
- [ ] 根图层集和派生图层集遵循唯一 ID、parent、递增 version 和稳定 digest 规则
- [ ] 图层画布节点不修改上游快照，布局变化、选择和上游 digest 均参与 `inputHash`
- [ ] 图层画布节点卡片显示组合预览、图层数、选中图层和修改数量
- [ ] 图层画布节点提供 `selected_layer` 与派生 `layers` 两个类型正确的输出端口
- [ ] 独立图层编辑路由可直达和刷新，并正确加载已保存 Pipeline 与最新成功上游图层集
- [ ] 图层编辑器支持选中、移动、等比缩放、排序、显隐、删除、撤销和重做
- [ ] 底图不可移动、缩放、隐藏、删除或作为图片编辑目标
- [ ] 主画布有未保存修改时不能直接进入图层编辑器，必须先保存 Pipeline
- [ ] 保存到节点只持久化目标节点配置、递增 Pipeline revision，且不自动运行
- [ ] Pipeline revision 冲突时保留本地图层草稿，不覆盖服务端 definition
- [ ] 上游 layer set digest 变化时旧选择和布局补丁标记过期且不自动套用
- [ ] 指定图层编辑结果保持来源 layer set ID、version、digest、layer ID、bbox 和变换上下文
- [ ] 指定图层编辑结果被规范化为原 bbox 像素尺寸的 PNG，并受原 alpha 蒙版约束
- [ ] 图层编辑失败时不修改原图层集或原图层资产
- [ ] 图层合成只接受一个来源完全匹配的 `edited_layer`
- [ ] 图层合成仅替换目标图层，保留其他图层资产、布局、显隐和顺序
- [ ] 图层合成输出新的派生 `layer_set` 和新的公共扁平图片资产
- [ ] 来源 ID、version、digest、layer ID 或像素尺寸不匹配时合成失败且无部分结果
- [ ] 可以串联多组图层画布、图片编辑和图层合成节点连续编辑多个图层
- [ ] 前后端节点注册表、端口类型、模式限制和序列化契约一致
- [ ] 后端 `.venv` 完整 pytest 通过
- [ ] 前端 lint、typecheck、完整 Vitest 和 production build 通过
- [ ] 浏览器完成三种模式、动态端口、图层编辑、保存恢复、指定图层替换和最终合成验收
- [ ] 桌面和移动视口下图层画布、工具栏、图层列表、文本和按钮无重叠或不可达
