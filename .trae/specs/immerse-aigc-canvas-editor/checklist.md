- [x] Pipeline 主画布不渲染全局导航、品牌标识或移动导航菜单
- [x] 模板主画布不渲染全局导航、品牌标识或移动导航菜单
- [x] AIGC 列表页及其他工作台页面继续保留全局导航和正确高亮
- [x] 图层编辑器与时间线编辑器的既有沉浸式 Shell 无回归
- [x] Pipeline、模板及其错误态使用完整 `100dvh`，页面无双层顶栏导致的纵向滚动
- [x] 画布工具栏是页面唯一顶栏，并保持稳定高度
- [x] 桌面工具栏清晰区分画布身份区和命令区
- [x] 工具栏展示当前画布名称与“画布 / 模板”身份，长名称单行截断且完整名称可访问
- [x] 已保存、保存中、待保存、失败和冲突状态均无可见展示，仅保留 `aria-live` 播报
- [x] 工具栏状态展示不触发额外保存或改变自动保存时序
- [x] Pipeline 保留详情、另存为模板和执行，执行仍是最明确的主命令
- [x] 模板不展示另存为模板或执行，且不遗留无意义分隔线
- [x] 仅图标按钮使用 Lucide 图标，并具有 `aria-label`、`title`、可见焦点和至少 `40px` 点击目标
- [x] 工具栏使用中性石墨背景、低对比边框和克制阴影，不使用装饰渐变或大面积高饱和底色
- [x] `1024px` 及以上视口展示完整工具栏信息且不挤压关键命令
- [x] `768px` 至 `1023px` 视口保持单行，次要命令文字收缩后仍可访问
- [x] `390px` 视口无水平溢出、文本重叠或按钮遮挡，返回、详情和执行始终可达
- [x] 长标题、保存错误与运行中状态不会改变工具栏高度
- [x] 节点库显隐、详情栏开合和底部视图工具坞无回归
- [x] 节点添加、选择、移动、缩放、连接、删除及 viewport 行为无回归
- [x] 返回、另存模板、执行和进入深层编辑器继续在操作前执行现有 `flush()`
- [x] 保存失败、草稿无效和 `409` 冲突继续阻止依赖最新 revision 的操作
- [x] 工具栏和 Shell 变更不修改 Pipeline definition 或持久化 UI 状态
- [x] 定向 Vitest、完整 Vitest、TypeScript、ESLint 和 production build 通过
- [x] Playwright 在 `1440x900`、`1024x768`、`768x1024` 和 `390x844` 完成截图与布局验收
- [x] Playwright 验收期间无新增浏览器控制台错误
