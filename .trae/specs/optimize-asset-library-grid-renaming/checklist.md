- [x] 宽度至少 1280px 时，每个资产分区一行展示 6 张卡片
- [x] 每页固定 30 项，宽屏按每行 6 项展示 5 行
- [x] 31 项及以上显示分页，第二页从第 31 项开始
- [x] 视口按 `<640=1、640～1023=2、1024～1279=3、>=1280=6` 列响应式展示，分页边界为 30 项
- [x] 角色、场景、图片成品、产物、工具和 AIGC 分区使用一致网格规则
- [x] 图片和视频缩略图继续使用 `object-contain`
- [x] 点击可用图片缩略图可打开大图预览
- [x] 放大图片不裁切、不拉伸
- [x] 无预览 URL 的资产不提供无效放大交互
- [x] 所有独立项目、工具和 AIGC 资产卡片显示重命名按钮
- [x] 派生尾帧卡片不显示独立重命名按钮
- [x] 宿主改名后尾帧仍固定显示“尾帧图”，但搜索可匹配宿主新名称
- [x] 重命名按钮与删除按钮不重叠，并具有 tooltip 与 aria-label
- [x] 重命名弹窗预填当前展示名并提供取消、保存操作
- [x] 空白、超过 120 个 Unicode code points 或包含 ASCII U+0000～U+001F / U+007F 控制字符的名称被拒绝
- [x] `PATCH /api/assets/{asset_id}` 可重命名公开项目资产
- [x] `PATCH /api/assets/{asset_id}` 可重命名公开工具和 AIGC 资产
- [x] PATCH 响应包含与资产列表一致的有效访问 URL，前端整体替换后预览仍可用
- [x] 不存在或非公开内部资产返回 404
- [x] 重命名只更新 `metadata.name` 与 `metadata.name_scheme=user_defined_v1`
- [x] 其他 metadata、对象 key、URL、归属、引用和缓存语义保持不变
- [x] 保存成功后卡片名称立即更新且无需刷新
- [x] 搜索、预览标题和默认下载文件名立即使用新名称
- [x] 刷新页面后仍显示持久化的新名称
- [x] 保存失败时弹窗保持打开、显示错误并保留原名称
- [x] 请求进行中禁止重复提交
- [x] 用户名称优先于 description、AIGC 自动名称和 prompt
- [x] 同名资产允许保存且仍按 asset ID 区分
- [x] 后端相关测试和完整 pytest 通过
- [x] 前端完整 Vitest、TypeScript、ESLint 和 production build 通过
- [x] Playwright 在 1440x1000、1280x800、1024x768、390x844 视口通过
- [x] Playwright 无控制台错误、横向溢出、控件重叠或媒体比例回归
