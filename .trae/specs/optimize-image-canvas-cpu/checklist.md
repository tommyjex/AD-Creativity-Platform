- [x] 已创建 `canvas-cpu-spike` 调试会话，且首个执行阶段未修改业务逻辑
- [x] 修复前证据覆盖空画布、含参考图节点画布和含待处理输出节点画布
- [x] 修复前采样包含打开后至少 10 秒空闲阶段及一轮核心画布交互
- [x] React 提交、节点状态更新、尺寸观测/布局测量、任务轮询和资源清理均有可核对的运行时证据
- [x] 所有候选假设均已标记为 CONFIRMED、REJECTED 或 INCONCLUSIVE，并引用具体日志或性能采样
- [x] 业务逻辑修复仅针对证据确认的根因，不包含无关重构
- [x] 修复后同环境、同项目、同路径的 10 秒稳态 CPU 不再持续接近 100%
- [x] 修复后稳态 CPU 中位值较修复前下降至少 70%，高频回调或状态更新在 2 秒内收敛
- [x] 平移、缩放、节点拖拽/缩放、bbox 绘制/删除后 CPU 能恢复稳态
- [x] 参考图和输出图保持原始宽高比，bbox 覆盖层位置与实际框选区域一致
- [x] 节点布局防抖保存、revision 冲突保护和输出任务轮询行为无回归
- [x] 前端 lint、typecheck、相关 Vitest 和 build 全部通过
- [x] 如触及后端，仓库根目录 `.venv` 下 pytest 回归通过
- [x] 用户已明确确认修复结果或中止调试
- [x] 用户确认后，调试服务器、全部插桩及 `canvas-cpu-spike` 临时调试文件已清理
