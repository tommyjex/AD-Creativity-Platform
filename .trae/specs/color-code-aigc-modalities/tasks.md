# Tasks

- [x] Task 1: 建立统一的 AIGC 模态色 token 与映射。
  - [x] SubTask 1.1: 在全局样式中定义文本蓝、图片绿、视频橙、音频玫红的主色、浅色和边框语义变量。
  - [x] SubTask 1.2: 新增基于 `AigcPortType` 返回 Handle、连线、边框、标题栏和图标样式的前端纯函数。
  - [x] SubTask 1.3: 为未知端口类型提供中性回退，并增加映射与对比度相关单元测试。

- [x] Task 2: 将模态色应用到端口和连线。
  - [x] SubTask 2.1: 将所有输入与输出 Handle 从方向色改为端口类型色。
  - [x] SubTask 2.2: 生视频节点的文本、图片、视频和音频端口分别使用四种模态色；模式禁用端口保留色相并降低透明度。
  - [x] SubTask 2.3: 文生图、图生图、LLM 和输出节点按端口类型使用同一映射。
  - [x] SubTask 2.4: 合法已建立连线按源端口类型着色，模式不兼容连线继续使用 destructive 红色。
  - [x] SubTask 2.5: 补充 Handle 类型、禁用状态、合法连线和错误优先级测试。

- [x] Task 3: 将模态色应用到四类输入节点卡片。
  - [x] SubTask 3.1: 文本、图片、视频和音频输入节点使用对应模态色边框、标题栏浅色背景和标题图标。
  - [x] SubTask 3.2: 保持模型节点、输出节点、媒体区、正文和运行状态现有视觉语义。
  - [x] SubTask 3.3: 保持选中 ring、尺寸、缩放和布局稳定，并补充节点样式测试。

- [x] Task 4: 完成回归与浏览器验收。
  - [x] SubTask 4.1: 运行 AIGC 聚焦 Vitest、完整前端 Vitest、lint、typecheck 和 production build。
  - [x] SubTask 4.2: 在真实画布验证生视频四模态端口、生图端口、四类输入卡片和合法/错误连线。
  - [x] SubTask 4.3: 在桌面与窄屏验证颜色、标签、图标、选中状态和布局无重叠或尺寸跳动。
  - [x] SubTask 4.4: 使用 DOM/CSS 取证验证 Handle 与背景对比度不低于 3:1，且关键信息不只依赖颜色。

- [x] Task 5: 修复窄屏 AIGC 编辑器顶部工具栏重叠。
  - [x] SubTask 5.1: 在 390×844 视口下调整编辑器标题、Revision/未保存徽标与操作按钮的响应式布局，避免标题区被压缩至不可读及控件视觉叠压。
  - [x] SubTask 5.2: 补充窄屏工具栏布局回归测试，并重新执行 390×844 浏览器验收，确认无重叠、溢出或不可读状态。

# Task Dependencies

- Task 2 和 Task 3 依赖 Task 1，可并行执行。
- Task 4 依赖 Task 2 和 Task 3。
- Task 5 依赖 Task 4 的窄屏验收结果。
