- [x] 画布页正文区为可平移/缩放的节点画布，铺满顶部导航下方区域且四周无留白
- [x] 采用 `@xyflow/react` 承载节点拖拽、缩放、平移与自定义节点渲染
- [x] 参考图节点支持本地上传与从资产库添加，单节点承载单张参考图
- [x] 参考图节点可自由拖拽位置、缩放大小，且缩放严格保持原始宽高比
- [x] 参考图节点按添加顺序编号为「图1、图2…」并显示编号徽标
- [x] 参考图节点内可画框提取归一化 `bbox`，提取后自动引用到右侧提示词编辑器，且引用坐标不可手工篡改
- [x] 清除框选或删除参考图节点时，右侧提示词编辑器中对应区域引用同步移除
- [x] 删除参考图节点前二次确认，且仅解除引用/选择，不删除后端原始资产文件
- [x] 输出节点展示文生图/参考图生图/图层拆分产物，含进行中状态与结果轮询
- [x] 输出节点支持下载、查看原图、设为参考图、图层拆分入口（跳转图层编辑页）
- [x] 输出节点图片严格保持原始宽高比，位置/尺寸可拖拽缩放并持久化
- [x] 提示词编辑器与图片配置（画幅/分辨率/格式）固定在右侧停靠面板，平移/缩放画布时保持固定
- [x] 生成模式按是否存在参考图区域引用自动判定（参考图生图 / 文生图），输入不完整时禁用并提示
- [x] 画布布局（节点类型/位置/尺寸/参考图引用与 bbox/输出引用）持久化到后端，刷新或换设备可恢复
- [x] 布局保存携带 `expected_revision`，冲突时不覆盖远端并提示刷新；成功后 revision 递增
- [x] 关闭画布仍导航回上一页；项目不存在或加载失败按既有范式返回未找到/错误提示
- [x] 后端 `.venv` pytest 回归通过；前端 `npm run lint`、`npm run typecheck`、`npm run test`、`npm run build` 全部通过
