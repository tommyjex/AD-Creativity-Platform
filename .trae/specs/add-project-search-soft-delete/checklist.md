- [x] `projects` 表包含可空 `deleted_at` 字段及活跃项目查询索引。
- [x] 既有数据库可通过幂等增量迁移获得软删除字段和索引，不丢失原有数据。
- [x] 内存仓储与 SQLAlchemy 仓储实现一致的项目搜索、软删除和已删除项目不可见语义。
- [x] `GET /api/projects?q=...` 能匹配项目名称、商品名称和广告需求，忽略大小写及首尾空格。
- [x] 未传或传入空白 `q` 时，项目列表返回全部未删除项目并保持现有排序。
- [x] `DELETE /api/projects/{project_id}` 首次删除返回 204，重复删除或删除不存在项目返回统一 404。
- [x] 软删除只更新项目删除标记，不物理删除 Brief、文本产物、角色卡、分镜、任务、资产记录或对象存储文件。
- [x] 已删除项目不出现在项目列表、项目详情、项目级资源接口和全局资产列表中。
- [x] 前端 API client 正确编码搜索参数、发送 DELETE 请求并处理 204 空响应。
- [x] 项目模块提供关键词搜索框、搜索加载态、搜索失败反馈和无结果空状态。
- [x] 项目列表项提供可访问的删除图标按钮，且不会误触项目选择。
- [x] 删除操作必须经过包含项目名称和数据保留说明的确认弹窗。
- [x] 删除成功后项目从当前列表消失；若删除当前项目，详情和选择状态同步清空。
- [x] 删除失败时项目与详情保持不变，弹窗展示错误并允许重试或取消。
- [x] 前端交互测试覆盖搜索、清空、无结果、取消删除、删除当前/非当前项目和删除失败。
- [x] 后端测试覆盖两种仓储的搜索、软删除、重复删除、详情隐藏、资产隐藏和关联数据保留。
- [x] 后端测试使用项目根目录 `.venv` 执行通过。
- [x] 前端测试、TypeScript 检查和 ESLint 执行通过。
- [x] 浏览器验证桌面与移动布局，搜索和删除交互无文本截断、控件重叠或焦点异常。
