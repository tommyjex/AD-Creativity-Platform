- [x] “画布模板”和“我的画布”的每个条目均提供独立删除图标，点击不会触发卡片主操作。
- [x] 删除模板或画布前使用应用内确认弹窗；取消或关闭不会发起删除请求。
- [x] 模板删除 API 成功返回 `204 No Content` 且响应体为空，不存在模板返回 `404 Not Found`。
- [x] Pipeline 删除 API 成功返回 `204 No Content` 且响应体为空，不存在 Pipeline 返回 `404 Not Found`。
- [x] Pipeline 仅有终态历史 Run 时软删除并从列表、普通详情中隐藏。
- [x] Pipeline 存在 queued/running Run 时删除返回 `409 Conflict`。
- [x] 软删除和 `409` 场景均不会级联删除历史 Run、RunNode、Task、任务资产关联和结果资产。
- [x] 删除模板不会删除或修改由其创建的既有画布，既有画布仍可打开、编辑、保存和运行。
- [x] 删除成功后重新获取并刷新用户当前所在的模板或画布列表。
- [x] 删除失败时不乐观移除条目，并保留当前视图、筛选条件、分页状态和列表内容。
- [x] `404`、`409`、网络错误和服务端错误均有用户可读反馈，提交期间不会重复发送请求。
- [x] MemoryRepository 与 MySQLRepository 契约测试覆盖物理删除、软删除、`404`、活动 Run `409`、禁止级联和模板实例保留。
- [x] API 测试覆盖两个删除接口的 `204` 空响应、`404`、`409` 及失败后的数据不变性。
- [x] 前端测试覆盖删除图标、事件隔离、确认/取消、成功刷新当前列表、失败保留和错误提示。
- [x] 后端 `.venv` 全量 pytest 通过。
- [x] 前端 lint、typecheck、完整测试和 production build 通过。
- [x] 浏览器验收覆盖模板删除、无 Run 画布删除、有历史 Run 画布的 `409`、失败保留、当前列表刷新及模板删除后既有画布可用。
