- [x] 只有文生图和图生图目标启用英文动态分层
- [x] LLM 和生视频提示词优化语言与结构无回归
- [x] 生图 Provider 内部响应使用 `sections + optimized_reference_instructions`
- [x] 公开 API 继续返回 `optimized_text + optimized_reference_instructions`
- [x] Provider 仅返回旧版 `optimized_text` 时生图优化被拒绝
- [x] 每次生图优化包含 4–10 个非空 section
- [x] section label 仅使用允许的 ASCII 字符并以英文字母开头
- [x] section label 大小写不敏感唯一
- [x] `Composition` 必须存在
- [x] `Negative Prompt` 必须存在且位于最后
- [x] 动态层级根据用户内容、优化方向、目标配置和参考图上下文选择
- [x] 不相关层不输出，不使用空层凑数量
- [x] 不因分层要求虚构人物、动物、产品、家具或其他对象
- [x] 室内需求可覆盖空间、家具、光影、人物、动物、产品和构图等相关层级
- [x] 商品需求使用产品、材质、背景、光影、镜头和构图等相关层级
- [x] 简单背景需求仍满足最少层级且不虚构主体
- [x] 图片编辑明确编辑动作和需保持不变内容
- [x] 普通图生图明确参考用途和需保留特征
- [x] 多参考图按 definition edge 顺序稳定描述对象、用途和关系
- [x] `Composition` 包含任务相关的景别、机位、位置、空间、留白或画幅信息
- [x] `Negative Prompt` 保留用户否定条件并只补充相关失败模式
- [x] `Negative Prompt` 不否定用户明确要求或参考图需保留内容
- [x] 后端以稳定的 `Label: content` 多行格式渲染
- [x] 最终文本不包含 Markdown、JSON、编号、解释或候选方案
- [x] 相同 sections 渲染为字节一致的文本
- [x] 生图主提示词的可翻译描述使用英文
- [x] 每条 BBox 引用说明的可翻译描述使用英文
- [x] 屏蔽受保护字面量后，中日韩文字会触发结果拒绝
- [x] 每个 label、content 和非空引用说明至少包含 ASCII 英文字母
- [x] BBox 引用数量、顺序、ID、token 和坐标保持不变
- [x] 引号内画面文字、URL、反引号、比例和带单位数字保持原 code point
- [x] 品牌、产品、型号、颜色、数量和否定条件保持
- [x] 任一主分层或引用说明失败时整次响应被拒绝
- [x] 失败时原主文本和全部引用说明保持不变
- [x] 优化成功一次性写回多行主文本和全部引用说明
- [x] 一次撤销可恢复优化前主文本和全部引用说明
- [x] 优化不自动保存 Pipeline
- [x] loading、取消、过期响应和无变化语义无回归
- [x] 优化 API 不创建 Pipeline Run 或 Task
- [x] 优化 API 不调用图片生成
- [x] 错误响应和日志不泄露 Provider 原始响应、系统提示词、密钥或堆栈
- [x] 固定语料覆盖室内、商品、人物、动物、纯背景和图片编辑
- [x] 后端 schema、Provider、服务、路由定向测试及完整 pytest 通过
- [x] 前端定向测试、完整 Vitest、TypeScript、ESLint 和 production build 通过
- [x] 浏览器验证室内、商品和图片编辑均输出英文动态分层
- [x] 浏览器验证中文画面文字、品牌/型号、BBox token 和坐标保持
- [x] `1440x900`、`1024x768`、`390x844` 多行文本无溢出、重叠或不可达操作
- [x] 浏览器控制台无新增错误且未调用真实计费生成接口
