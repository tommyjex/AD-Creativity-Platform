- [x] 文本、图片、视频和音频输入节点分别使用“文本节点 / 图片节点 / 视频节点 / 音频节点”基础显示名
- [x] 同一基础显示名只有一个节点时不显示编号
- [x] 同一基础显示名有多个节点时按 `definition.nodes` 顺序显示连续的 1-based 编号
- [x] 新增同类节点追加到编号末尾，删除节点后剩余编号自动连续重排
- [x] 拖拽或缩放节点不改变编号
- [x] Seedream operation 等模式相关标题按当前基础显示名分别分组编号
- [x] 编号仅为派生 UI，不修改节点 ID、Pipeline definition、模板 schema、连线、运行快照或缓存语义
- [x] 画布节点标题与右侧配置分组标题使用相同派生显示名
- [x] 精准框选目标列表使用“文本节点N”而不是内部 ID
- [x] BBox 引用 token 使用“图片节点N”而不是截断 UUID
- [x] 基础文本、BBox token 和引用说明位于同一个提示词编辑面
- [x] 页面不再显示独立“框选引用”卡片区
- [x] 无 BBox 引用时统一编辑面保持普通基础文本编辑行为
- [x] 新绑定引用按 `bbox_references` 数组顺序追加到统一编辑面
- [x] BBox token 紧凑显示来源节点派生名和当前 `bbox x1 y1 x2 y2`
- [x] BBox token 坐标、来源和运行时图片编号不可手工修改
- [x] token 移除操作可访问，且只删除当前文本节点引用，不清除源图片 BBox 或其他文本节点引用
- [x] 每个 token 后的说明可编辑，并只更新对应 `source_node_id` 的 `instruction`
- [x] 引用资产加载中或失效时在编辑面原顺序位置显示紧凑状态，不阻断其他内容
- [x] 手工输入 `<bbox>` 或 `<point>` 继续被拒绝，最后有效提示词不被覆盖
- [x] 现有每节点最多 10 条引用、同源唯一和 Unicode 说明长度限制保持不变
- [x] 重新框选、清除 BBox、更换图片、删除节点和删除关联边继续同步引用
- [x] 提示词优化只更新基础文本和引用说明，不改变 token 来源、顺序、坐标或数量
- [x] 优化期间编辑和删除禁用，过期响应不覆盖新内容，一次撤销恢复完整旧提示词
- [x] 现有运行时 `图N<bbox>…</bbox>` 编译顺序、inputHash 和模板净化行为不变
- [x] 节点显示名、提示词编辑器、画布节点和详情栏定向测试通过
- [x] 前端完整 Vitest、TypeScript、ESLint 和 production build 通过
- [x] `1440x900`、`1024x768` 和 `390x844` 浏览器验收无文本/控件重叠且详情内容可达
- [x] 浏览器控制台无新增错误，单节点、无 BBox 和普通工作区无回归
