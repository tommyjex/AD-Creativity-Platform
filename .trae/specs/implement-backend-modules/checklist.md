- [x] 后端存在可导入的 FastAPI `app` 对象。
- [x] `GET /health` 返回服务状态、应用名称和版本信息。
- [x] 后端依赖配置包含 FastAPI、Pydantic、测试工具和 ModelArk SDK 预留依赖。
- [x] 核心 schema 覆盖项目、brief、文本产物、分镜、任务和资产。
- [x] 统一枚举覆盖阶段、状态、资产类型和错误码。
- [x] 项目创建、详情查询和资产查询 API 可用。
- [x] 故事、剧本、分镜、生图、生视频和剪辑阶段 API 可用。
- [x] 任务查询和失败任务重试 API 可用。
- [x] 工作流实现阶段依赖校验、任务幂等保护和下游 `stale` 标记。
- [x] ModelArk mock 适配层能返回可预测的文本、图片、视频和成片结果。
- [x] 自动化测试覆盖 schema、健康检查、项目 API、任务 API、工作流和 mock 生成。
- [x] 后端测试命令执行通过。
