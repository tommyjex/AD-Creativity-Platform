- [x] 右侧不再显示单个“当前任务”详情，而是仅包含全模态参考生视频任务的可点击任务列表。
- [x] 每个任务条目显示提交时使用的可读模型名称；缺少模型字段的历史任务显示“未记录”。
- [x] 新提交生成任务会加入列表并自动选中；重试创建的新任务也会自动选中。
- [x] 列表条目显示创建时间、状态、提示词摘要、参考视频数量和明确选中态，且最新任务优先。
- [x] 点击任务后，左侧参考视频来自该任务快照，生成结果来自该任务输出资产。
- [x] 选中任务成功后生成结果窗口自动可播放；没有参考、运行中、失败或没有输出时显示正确空状态。
- [x] 选中任务可播放视频保留 controls、放大预览和 `object-contain`。
- [x] 失败任务重试后保留原任务并自动选择新任务。
- [x] 前端相关 Vitest、lint 和 typecheck 通过。
- [x] 浏览器验收确认列表点击、播放器切换、播放/放大和响应式布局正确。
