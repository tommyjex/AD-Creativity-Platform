- [x] 图生图节点图片端口允许连接 1 至 10 个 `image_asset` 上游
- [x] 第 11 条图片连线在前端被拒绝，并显示明确的中文上限提示
- [x] 绕过前端提交超过 10 条图片输入的 definition 时，后端校验失败并定位目标节点
- [x] 相同起点、端口和终点的完全重复边在前后端均被拒绝
- [x] 图生图提示词端口及其他现有端口仍保持单值输入
- [x] 图生图节点卡片显示准确的 `参考图 n/10`
- [x] 达到 10 张时，图片输入端口提供已达上限的可访问提示
- [x] 多参考图按 definition 边顺序解析并写入任务快照
- [x] 任务快照只保存资产 ID，不保存临时签名 URL
- [x] 第一张参考图作为主图，其余最多 9 张作为附加参考图传给模型
- [x] 图片输入节点和上游图片模型输出可混合作为参考图来源
- [x] 每条连线只贡献第一张可用图片，且不同连线解析到相同资产时不被静默去重
- [x] 任一参考资产不可用时整项任务失败，不发送部分模型请求
- [x] 所有输入图均写入 `pipeline_task_assets`，且 `ordinal` 与模型输入顺序一致
- [x] 参考图顺序变化会改变 `inputHash`，不会错误复用旧结果
- [x] 图片执行器版本已升级，旧单图缓存不会跨版本复用
- [x] 旧单图画布无需迁移即可继续保存和运行
- [x] 旧 `source_asset_id` 任务快照可恢复，新任务只写 `reference_asset_ids`
- [x] definition 保持 `schemaVersion=1`，节点、边和数据库表结构无需迁移
- [x] 后端完整 pytest 通过
- [x] 前端 lint、typecheck、完整 Vitest 和 production build 通过
- [x] 浏览器完成 1–10 张连接、超限提示、计数、保存恢复和执行验收
