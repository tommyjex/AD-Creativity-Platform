- [x] 工作台项目详情存在分镜视频生成模块，并采用左右两栏布局。
- [x] 左栏按镜头编号展示结构化分镜列表。
- [x] 用户切换左栏分镜后，右栏展示对应分镜的视频状态和预览。
- [x] 每个分镜支持编辑并保存独立的视频生成提示词。
- [x] 未保存过视频提示词的分镜能展示基于分镜内容生成的默认提示词。
- [x] 每个分镜支持上传本地参考图，并将文件上传到 TOS、创建资产记录、关联到该分镜。
- [x] 每个分镜支持上传本地参考视频，并将文件上传到 TOS、创建资产记录、关联到该分镜。
- [x] 每个分镜支持上传本地参考音频，并将文件上传到 TOS、创建资产记录、关联到该分镜。
- [x] 上传文件类型或大小非法时，系统拒绝上传且不创建资产记录。
- [x] 每个分镜支持从资产库选择参考图并建立关联。
- [x] 每个分镜支持从资产库选择参考视频并建立关联。
- [x] 每个分镜支持从资产库选择参考音频并建立关联。
- [x] 用户移除参考素材时，只解除分镜关联，不删除资产库文件。
- [x] 单分镜视频生成 API 使用该分镜最新提示词、时长、画面比例、参考图、参考视频、参考音频和 brief。
- [x] 单分镜视频生成成功后创建 `storyboard_video` 资产，并更新对应 `StoryboardShot.video_asset_id`。
- [x] 单分镜视频生成失败时只影响当前分镜任务，不影响其他已生成分镜视频。
- [x] 分镜视频生成失败时 API 和前端错误提示不暴露密钥、堆栈或供应商原始敏感错误。
- [x] 右栏在已有视频时展示可播放视频、状态、更新时间和基础元数据。
- [x] 右栏在未生成视频时展示空状态、生成入口和当前配置摘要。
- [x] 右栏在失败时展示可理解失败提示和重试入口。
- [x] 后端测试覆盖配置保存、素材上传、素材关联、移除关联、单分镜生成、失败重试和错误脱敏。
- [x] 前端测试覆盖两栏布局、分镜切换、提示词编辑、本地素材添加、资产库素材选择、视频预览和失败重试。
- [x] API client 测试覆盖新增上传、配置保存、素材关联和单分镜生成接口。
- [x] `.venv` 后端测试通过，前端 lint、typecheck、test、build 通过。
- [x] 本地 smoke test 能完成“进入分镜视频模块 -> 编辑提示词 -> 添加参考素材 -> 生成单分镜视频 -> 右侧查看视频”。
