- [x] 节点面板只提供文本、图片、视频、音频四类模态节点，不再提供独立输入/输出节点
- [x] 模态节点各有一个可选同模态单值输入端口和一个同模态输出端口
- [x] 模态节点不创建 Task Attempt，但每个计划内节点都有 RunNode
- [x] 无入边时文本节点使用本地文本，媒体节点使用本地资产
- [x] 有入边时严格以上游结果为有效值，不合并或回退本地内容
- [x] 连接上游时本地内容完整保留但所有本地编辑操作禁用
- [x] 上游模式备用配置仍满足字段结构约束，备用本地资产继续受 `pipeline_assets` 保护
- [x] 断开上游后原本地文本、资产和合法 BBox 自动恢复
- [x] 第二条入边在前端连接、后端保存和运行阶段均被拒绝
- [x] 模态输出可连接多个下游，并允许无环的同模态节点链
- [x] 自环与任意间接环路继续被前后端拒绝
- [x] 本地模式模态节点在 Run 初始化时产生无任务结果快照
- [x] 上游模式模态节点在依赖成功或复用后即时投影并释放下游
- [x] 中继节点透传同一文本或资产 ID，不复制资产
- [x] 文本中继不创建文本资产或任务，但在自身 RunNode 保存不可变透传快照
- [x] 上游失败、取消、超时或 unavailable 时节点及后代正确传播状态且不回退本地值
- [x] 从模态节点继续执行包含必要上游与下游且不为模态节点创建任务
- [x] 本地文本节点保留基础文本、BBox token、说明和提示词优化
- [x] 上游文本节点展示所选 Run 文本、支持复制且不编译本地 BBox
- [x] 本地图片节点保留上传、资产选择、预览和精准框选
- [x] 上游图片节点保持原图比例预览，支持原图和下载且禁用本地框选
- [x] 本地视频和音频节点保留上传、资产选择和播放
- [x] 上游视频支持播放、全屏、元数据、下载和继续透传
- [x] 上游音频支持播放、时长、MIME、下载和继续透传
- [x] 等待、失败和不可用上游结果不会显示本地内容冒充结果
- [x] 空本地值可保存；被模型消费时运行失败，未消费终端保持 idle，纯模态 Pipeline 不可执行
- [x] 结果面板按选中 Run 的 definition snapshot 与 RunNode 展示四类模态结果，不混用当前 definition、其他 Run 或本地备用内容
- [x] 终端模态节点无需下游即可展示结果，且不改变模型任务汇总语义
- [x] 四类节点沿用文本蓝、图片绿、视频橙、音频玫红的模态色
- [x] 自动编号按文本/图片/视频/音频分别计算，单节点不显示编号
- [x] 节点卡片和配置栏明确显示“本地 / 上游”状态及上游优先说明
- [x] `schemaVersion=1` 的输入和输出节点按规定映射为 v2 模态节点
- [x] v1→v2 迁移保留 node/edge ID、位置、尺寸、连线、输入配置和输出标题
- [x] 文本、图片、视频、音频各类 v1 配置按字段矩阵无损迁移并补齐确定性默认值
- [x] v1→v2 迁移幂等，保存后持久化 `schemaVersion=2`
- [x] 缺失版本按 v1 迁移，未知版本和混合 v1/v2 模态类型被明确拒绝
- [x] 历史 Run definition snapshot 不被回写，v1 历史结果可只读展示
- [x] 非法 v1 definition 的类型、重复输入和环路错误不会被迁移掩盖
- [x] 模板清除图片、视频、音频节点本地 asset，图片同时清除 BBox
- [x] 模板同步清除指向已删除图片 BBox 的文本引用、token 和说明，不产生悬空引用
- [x] 上游模式媒体节点的备用本地资产也会在模板中清除
- [x] `pipeline_assets` 只维护本地媒体引用，上游透传不创建虚假引用
- [x] 模型任务的 `pipeline_task_assets` 输入输出追溯保持完整
- [x] 本地有效内容变化会使下游 `inputHash` 变化
- [x] 上游模式未生效备用内容变化不会使下游 `inputHash` 变化
- [x] BBox 仅允许本地模式图片与文本节点建立、同步和编译
- [x] 文本或图片切为上游模式时相关 BBox 引用暂停，双方恢复本地模式后重新生效
- [x] 普通图生图 BBox 的同源唯一、最多 10 条、顺序和安全标签规则保持不变
- [x] Pipeline/template 创建、更新、实例化和执行入口兼容 v1/v2
- [x] 默认模板、内置模板、空白画布和验收 fixture 均使用 v2 模态节点
- [x] 前后端节点注册表、联合类型、端口和迁移结果保持同构
- [x] LLM、图片模型、视频模型、增强、打码和图层工作流行为无回归
- [x] 后端迁移、DAG、执行器、模板、资产、API 定向测试及完整 pytest 通过
- [x] 前端迁移、Store、节点、BBox、投影测试及完整 Vitest、TypeScript、ESLint、构建通过
- [x] 隔离 v2 Pipeline 覆盖四模态本地输入、终端输出和中继链路
- [x] v1 fixture 覆盖自动迁移、v2 保存、历史 Run、标题和连线保持
- [x] `1440x900`、`1023x768`、`390x844` 浏览器验收无重叠、溢出或不可达操作
- [x] 音频输出通过“本地音频 → 音频节点”中继链完成播放、下载和继续透传验收
- [x] 浏览器控制台无新增错误，复制、预览、播放、全屏、下载和 BBox 操作可用
