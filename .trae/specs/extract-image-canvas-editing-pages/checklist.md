- [x] `/projects/[projectId]/canvas` 路由存在，服务端预取项目数据，直达/刷新可正常渲染画布编辑界面。
- [x] `/projects/[projectId]/canvas/layers/[layerSetId]` 路由存在，服务端预取图层集，直达/刷新可正常渲染图层编辑界面。
- [x] 画布编辑页功能与原「进入画布」对话框完全一致（目标图、参考图上传/移除/设为目标、指令提交、分辨率/格式、图层拆分入口、任务轮询与反馈）。
- [x] 图层编辑页功能与原图层编辑对话框完全一致（拖拽、等比缩放、层级、显隐、保存、导出成品、AI 替换图层内容、revision 冲突处理、未保存二次确认）。
- [x] 画布页「图层拆分」命中已存在图层集时导航到对应图层编辑路由；拆分任务完成后导航到新图层集路由并保留反馈提示。
- [x] 图片项目详情「进入画布」改为导航到画布路由；详情页不再内联渲染 ImageCanvasEditor / LayerDecomposeDialog / LayerEditorDialog。
- [x] 关闭画布页返回上一页；有未保存图层改动时关闭沿用既有二次确认后返回画布页。
- [x] 项目或图层集不存在时按既有范式返回未找到/错误，无未捕获异常。
- [x] 前端 `npm run lint`、`typecheck`、相关 `vitest`、`build` 全部通过。
