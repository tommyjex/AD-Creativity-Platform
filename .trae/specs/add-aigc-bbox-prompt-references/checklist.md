- [ ] 图片输入节点有资产时提供独立“精准编辑”入口，缩略图原图预览行为不变
- [ ] 精准编辑弹窗在桌面视口内完整显示，原图不拉伸、不裁切
- [ ] BBox 基于实际图片内容区使用 0–1000 归一化坐标
- [ ] BBox 与 `bbox_asset_id` 同时为空或同时有效，后端拒绝资产不匹配的框选
- [ ] 每个图片输入节点最多保存一个 BBox，重新框选替换旧框
- [ ] 框选、重置和目标绑定作为完整历史操作支持撤销重做
- [ ] 目标列表只允许选择与图片节点共同直接连接到同一图生图节点的文本节点
- [ ] 合法目标至少有一个直接下游，且所有直接下游均为接收该图片的图生图节点
- [ ] 给带引用文本节点新增不兼容下游边时前端拒绝，删除共享关系时自动清理失效引用
- [ ] 用户可将同一 BBox 一次引用到多个合法文本节点
- [ ] 同一文本节点对同一图片节点最多保留一条引用
- [ ] 满 10 条引用时仍可保留或取消已有同源引用，但不能新增第 11 条
- [ ] 文本编辑器展示框选缩略图、图片节点名称和当前坐标
- [ ] 用户可编辑基础文本和每条引用后的说明，但不能编辑图片编号与 BBox 坐标
- [ ] 引用说明长度以前后端一致的 Unicode code points 口径限制为 4000
- [ ] 后端拒绝用户文本中手工输入的 `<bbox>` 或 `<point>` 标签
- [ ] 重新框选后所有引用卡片自动显示新缩略图和坐标
- [ ] 清除 BBox、更换图片或删除图片节点时，全部关联引用自动移除
- [ ] 删除共同下游关系后，仅清理不再满足严格关联的引用
- [ ] 删除单个文本引用不清除源图片 BBox，也不影响其他文本节点
- [ ] 运行时按目标图生图节点的参考图顺序编译正确的 `图N<bbox>…</bbox>`
- [ ] 最终 prompt 严格按基础文本、引用标签、引用说明的数组顺序以单个空格连接
- [ ] 同一文本节点服务不同合法图生图目标时，各自使用目标相关的图片编号
- [ ] BBox、引用顺序、说明或图片编号变化会改变 `inputHash`
- [ ] 图片执行器版本升级，旧缓存不会错误复用
- [ ] 旧画布以空 BBox 和空引用加载，原有纯文本提示词行为不变
- [ ] 保存为模板会清除资产 ID、BBox 和 BBox 引用，但保留普通文本与拓扑
- [ ] 模板模式、无资产或不可访问资产时不能创建精准框选
- [ ] 后端完整 pytest 通过
- [ ] 前端 lint、typecheck、完整 Vitest 和 production build 通过
- [ ] 桌面浏览器完成框选、绑定、同步、保存恢复和实际运行验收
- [ ] 窄屏浏览器中弹窗内容可达且无图片裁切、文字或控件重叠
