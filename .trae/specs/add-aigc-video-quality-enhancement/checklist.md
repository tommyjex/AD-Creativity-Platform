- [x] AIGC 节点面板可添加“视频画质增强”节点，旧 `schemaVersion=1` 画布仍可加载。
- [x] 节点只接受一个 `video_asset` 输入并输出一个 `video_asset`，非法或缺失连接会在调用供应商前失败。
- [x] 默认配置为标准版、AIGC 场景、高清风格、1080p、保持原帧率、中码率档位和 8-bit。
- [x] 标准版支持五种场景且不发送色深；专业版不发送场景并支持 8/10/12/16-bit。
- [x] `resolution` 与 `resolution_limit` 互斥，预设值和短边 `[128,4320]` 范围校验正确。
- [x] 保持原帧率时不发送 `fps`，自定义帧率限制为 `[15,120]`。
- [x] 码率档位与精确码率互斥，精确码率限制为 `[10,150000]` kbps。
- [x] 专业版 16-bit 输入限制为 40 秒以内，且不发送码率参数并按 MOV 结果处理。
- [x] Gateway 校验公开视频资产、MIME、可访问性以及输入长短边范围，并记录输入资产引用。
- [x] MediaKit 请求使用现有 API Key、正确端点、白名单参数和稳定 `client_token`，不泄露密钥。
- [x] MediaKit 轮询正确处理运行、完成、失败、未知状态、非法响应和超时。
- [x] 增强结果在临时地址失效前以流式方式转存，不将大型视频完整缓存在内存。
- [x] MP4/MOV 输出资产状态、角色、类型、关联和增强元数据正确，失败时无空资产或残留引用。
- [x] 画质增强使用独立并发和超时配置，不占用 Seedance 并发槽位。
- [x] 参数与输入变化会使缓存失效；相同 input hash 且资产可用时复用成功结果。
- [x] 重试、取消、Worker 租约、晚到结果保护、从节点继续和独立分支收敛符合现有 AIGC 语义。
- [x] 配置面板动态显示适用字段，节点摘要清晰，高成本配置有明确标识且布局无溢出。
- [x] 结果面板和视频输出节点可播放、全屏、下载增强结果，并保持视频原始宽高比。
- [x] 运行日志展示状态、时间、耗时、Attempt 次数和脱敏失败原因。
- [x] 后端相关 pytest 在根目录 `.venv` 环境通过。
- [x] 前端相关 Vitest、typecheck、lint 和 production build 通过。
- [x] 浏览器 Mock 验收覆盖添加、连线、配置、保存重载、执行、失败重试、预览、全屏和下载。
- [x] 自动化测试和浏览器验收未调用真实计费的 MediaKit 画质增强 API。
