- [x] 顶级导航包含“工具”，并能打开独立页面 `/workspace/tools`。
- [x] 工具页采用“视频人物打码 / 全模态参考生视频”双标签页布局。
- [x] `ToolTask` 可持久化任务类型、状态、输入快照、供应商任务 ID、脱敏错误和完成时间，且不要求项目归属。
- [x] 工具上传参考素材、人物打码结果和生视频结果均作为工具资产保存，并关联对应工具任务。
- [x] 既有项目资产继续要求 `project_id`，项目详情、画布和分镜工作流通过回归测试。
- [x] 人物打码仅暴露马赛克/模糊和低/中/高强度；MediaKit 检测置信度和扩展比例使用 API 默认值。
- [x] 人物打码调用 MediaKit 异步任务，成功后将结果视频转存至本系统对象存储，可预览和下载。
- [x] 全模态生视频仅允许以下模型：`doubao-seedance-2-5-260628`、`doubao-seedance-2-0-260128`、`doubao-seedance-2-0-fast-260128`、`doubao-seedance-2-0-mini-260615`。
- [x] 全模态生视频支持提示词、参考图、参考视频、参考音频，且不支持首帧、尾帧或首尾帧控制。
- [x] 全模态生视频只允许时长 `4/8/12` 秒、画幅 `16:9/9:16/1:1`，固定 `720p` 且开启音频生成。
- [x] 两个工具都能持久化显示 `queued`、`running`、`succeeded` 和 `failed` 状态，并基于输入快照重试失败任务。
- [x] 供应商错误仅返回白名单化错误码、请求 ID、供应商任务 ID和阶段信息，不泄露密钥、签名 URL、原始错误体或堆栈。
- [x] 资产库支持按项目资产/工具资产来源筛选；工具资产显示工具类型、输入/输出角色、任务状态和创建时间。
- [x] 工具资产支持预览、下载和删除，删除不会影响同任务其他资产或任一项目资产。
- [x] 后端相关 pytest 在根目录 `.venv` 中通过。
- [x] 前端 test、lint、typecheck、build 通过，且浏览器验收覆盖人物打码和全模态生视频的成功、失败与重试状态。
- [x] “视频人物打码”标签页中，“当前任务”卡片显示在“人物打码配置”下方。
- [x] 人物打码页面右侧显示打码前和打码后视频播放窗口，且视频不被裁切或拉伸。
- [x] 打码前和打码后视频播放器支持放大预览，弹窗内视频保持原始宽高比并可关闭。
- [x] 资产库来源筛选为“工具资产”时点击“筛选”不会报错，重复点击仍保持正常工具资产视图。
- [x] 前端测试覆盖人物打码新布局、播放器放大预览和工具资产筛选 bug；lint 通过。
- [x] “人物打码配置”卡片中的输入视频、打码方式、打码强度在桌面宽度下横向显示在同一行。
- [x] 配置卡片下方为两栏式布局：左栏是人物打码任务列表，右栏是打码前后视频对比播放模块。
- [x] 任务列表支持选中历史任务；只有选中任务才驱动右侧加载对应打码前和打码后视频。
- [x] 上传新视频后自动清空已选历史任务和旧打码后视频，打码前播放器立即预览新上传视频。
- [x] 人物打码任务完成后，打码后视频卡片自动刷新为当前任务输出视频。
- [x] 前端测试覆盖横向配置、任务列表选中、上传新视频重置播放上下文、任务完成后输出视频刷新；typecheck 和 lint 通过。
