- [x] 文生图节点提供“分辨率档位 / 自定义像素”两种互斥尺寸方式
- [x] 普通图生图模式提供“分辨率档位 / 自定义像素”两种互斥尺寸方式
- [x] 旧节点的 `1K / 1.5K / 2K` 配置无需迁移并保持原行为
- [x] 自定义尺寸规范保存为无空格、小写 `x` 分隔的 `WIDTHxHEIGHT`
- [x] 自定义宽高仅接受十进制正整数
- [x] 自定义尺寸总像素范围按闭区间 `921600–4624220` 校验
- [x] 自定义尺寸宽高比范围按闭区间 `1:16–16:1` 校验
- [x] 前后端对格式、像素和比例使用一致的校验规则与边界
- [x] `2048x1024` 等合法尺寸可保存和运行
- [x] `512x512`、超最大像素、超比例和非整数值均被拒绝且不调用供应商
- [x] 切换到自定义像素时按当前档位与画幅初始化宽高
- [x] 自定义模式显示宽度、高度和输出格式，隐藏或禁用画幅
- [x] 自定义尺寸非法时显示可定位错误，并且不静默执行旧尺寸
- [x] 切回预设模式使用 `2K`，恢复原画幅并继续支持 `1K / 1.5K / 2K`
- [x] 自定义模式供应商 `size` 与节点规范尺寸完全一致
- [x] 自定义模式最终提示词不追加旧 `aspect_ratio`
- [x] 预设模式继续将档位传给供应商并保留现有画幅提示词
- [x] 自定义尺寸进入不可变任务参数快照和资产元数据
- [x] 修改宽或高会改变 `inputHash`，不会错误复用其他尺寸缓存
- [x] 失败重试使用原任务冻结尺寸，不读取节点当前尺寸
- [x] 真实与 mock ModelArk 适配器接受相同尺寸契约并返回一致元数据
- [x] 自定义尺寸结果实际宽高与目标完全一致后才保存成功资产
- [x] 输出尺寸不匹配或无法解码时任务失败且无成功资产、缓存或残留对象
- [x] 图片编辑模式不显示或接受自定义尺寸，切入时尺寸收敛为 `2K`
- [x] 图层拆分模式不显示或接受自定义尺寸，切入时尺寸收敛为 `auto`
- [x] 普通图生图的参考图数量、提示词、BBox 引用、格式和端口规则保持不变
- [x] Pipeline `schemaVersion`、数据库结构和现有 API 路径保持不变
- [x] 后端自定义尺寸定向测试、相关回归和完整 pytest 通过
- [x] 前端定向测试、完整 Vitest、TypeScript、ESLint 和 production build 通过
- [x] 隔离 Pipeline 的 `2048x1024` 文生图和普通图生图真实或受控 smoke test 通过
- [x] `1440x900`、`1024x768` 和 `390x844` 浏览器验收无控件不可达、文本重叠或横向溢出
- [x] 旧 Pipeline、图片编辑、图层拆分、BBox 引用和预设档位无回归，浏览器控制台无新增错误
