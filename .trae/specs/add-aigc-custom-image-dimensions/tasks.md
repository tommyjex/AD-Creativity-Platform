# Tasks

- [x] Task 1: 建立前后端共享的 Seedream 自定义尺寸契约。
  - [x] SubTask 1.1: 定义预设尺寸与规范 `WIDTHxHEIGHT` 联合类型，保持旧 `1K / 1.5K / 2K` 数据兼容。
  - [x] SubTask 1.2: 实现前端纯函数，解析、规范化并以整数运算校验正整数、总像素 `921600–4624220` 和宽高比 `1:16–16:1`。
  - [x] SubTask 1.3: 实现后端等价的自定义尺寸类型与校验器，拒绝空格、大小写分隔符、前导符号、小数、零值、越界像素和越界比例。
  - [x] SubTask 1.4: 为合法边界、非法格式、总像素边界和比例边界增加前后端参数化测试。

- [x] Task 2: 扩展 AIGC 节点配置与模式兼容规则。
  - [x] SubTask 2.1: 允许文生图配置和普通图生图配置保存预设档位或合法自定义尺寸，保持 `schemaVersion=1`。
  - [x] SubTask 2.2: 图片编辑继续只接受 `1K / 1.5K / 2K`，图层拆分继续只接受 `auto / 1K / 1.5K / 2K`。
  - [x] SubTask 2.3: 普通图生图携带自定义尺寸切换到图片编辑时收敛为 `2K`，切换到图层拆分时收敛为 `auto`。
  - [x] SubTask 2.4: 增加旧 definition 加载、模式切换、非法模式尺寸和前后端序列化一致性测试。

- [x] Task 3: 实现文生图和普通图生图的尺寸配置交互。
  - [x] SubTask 3.1: 在文生图和普通图生图配置中增加“分辨率档位 / 自定义像素”分段控件。
  - [x] SubTask 3.2: 预设模式保留现有画幅、尺寸和格式控件；自定义模式显示宽度、高度和格式，隐藏或禁用画幅。
  - [x] SubTask 3.3: 切换到自定义模式时按当前档位与画幅映射初始化宽高；切回预设模式时使用 `2K` 并恢复原画幅。
  - [x] SubTask 3.4: 对输入中的缺失、非整数、总像素和比例错误提供就地反馈，非法草稿不得静默使用旧尺寸执行。
  - [x] SubTask 3.5: 图片编辑和图层拆分配置不显示自定义尺寸入口。
  - [x] SubTask 3.6: 添加文生图、普通图生图、模式切换、键盘输入、错误反馈和窄屏布局组件测试。

- [x] Task 4: 将自定义尺寸接入任务解析、供应商请求与缓存。
  - [x] SubTask 4.1: 任务参数解析保留规范 `WIDTHxHEIGHT`，运行前再次执行后端权威校验。
  - [x] SubTask 4.2: 自定义模式将 `size` 原样传给 Seedream，且最终提示词不追加旧 `aspect_ratio`；预设模式保持现有画幅提示词。
  - [x] SubTask 4.3: 将规范尺寸写入任务快照、资产元数据和 `inputHash`，宽或高变化必须使缓存失效。
  - [x] SubTask 4.4: 失败重试复用原任务冻结尺寸，不读取节点当前值。
  - [x] SubTask 4.5: 扩展真实与 mock ModelArk 适配器类型，确保二者回填一致的尺寸元数据。
  - [x] SubTask 4.6: 添加文生图、单/多参考图生图、供应商 payload、提示词互斥、快照、缓存和重试测试。

- [x] Task 5: 校验自定义尺寸任务的实际输出。
  - [x] SubTask 5.1: 在持久化成功资产前读取下载结果宽高，仅对自定义尺寸要求与目标完全一致。
  - [x] SubTask 5.2: 输出不匹配时返回稳定错误阶段和可读信息，不创建成功资产或可复用缓存。
  - [x] SubTask 5.3: 失败时清理临时对象、部分资产与任务资产关联，预设档位的既有内容校验保持不变。
  - [x] SubTask 5.4: 添加 PNG/JPEG 正确尺寸、尺寸不匹配、解码失败、上传失败和数据库失败清理测试。

- [x] Task 6: 完成回归与浏览器验收。
  - [x] SubTask 6.1: 运行后端自定义尺寸、DAG、执行器、网关与 ModelArk 定向测试及完整 pytest。
  - [x] SubTask 6.2: 运行前端尺寸工具、AIGC 编辑器定向测试、完整 Vitest、TypeScript、ESLint 和 production build。
  - [x] SubTask 6.3: 使用隔离 Pipeline 验证 `2048x1024` 文生图和普通图生图请求、结果实际尺寸、元数据和缓存失效。
  - [x] SubTask 6.4: 在 `1440x900`、`1024x768` 和 `390x844` 验证尺寸模式切换、宽高输入、错误反馈、控件可达及无文本重叠。
  - [x] SubTask 6.5: 验证旧 Pipeline、图片编辑、图层拆分、BBox 引用和预设档位运行无回归，浏览器控制台无新增错误。

# Task Dependencies

- Task 2 依赖 Task 1。
- Task 3 依赖 Task 1、Task 2。
- Task 4 依赖 Task 1、Task 2，可与 Task 3 并行。
- Task 5 依赖 Task 4。
- Task 6 依赖 Task 3、Task 4、Task 5。
