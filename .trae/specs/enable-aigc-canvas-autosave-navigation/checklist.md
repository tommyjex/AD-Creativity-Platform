- [x] Pipeline 画布返回按钮导航到 `/workspace/aigc?view=pipelines`，并默认显示“我的画布”
- [x] 模板编辑器返回按钮导航到 `/workspace/aigc?view=templates`
- [x] `/workspace/aigc` 对缺失或非法 `view` 参数仍默认显示“画布模板”
- [x] Pipeline 和模板编辑器均不再显示常驻手动保存按钮
- [x] 名称、描述、节点、连线、位置、尺寸、配置和视口变更均可触发自动保存
- [x] 连续编辑经过 `800ms` 防抖后只提交最新草稿，不为每个中间状态发请求
- [x] 自动保存严格单飞，保存期间的新变更在当前请求成功并更新 revision 后续存
- [x] 旧保存响应不会把请求发出后产生的新变更误标记为已保存
- [x] 非持久化 UI 状态变化不会触发自动保存
- [x] 标题区域准确展示等待、保存中、已保存 revision、失败、冲突和无效状态
- [x] 网络或 `5xx` 按 `1s / 2s / 4s` 最多自动重试 3 次，后续编辑或 `online` 可恢复保存
- [x] `409` 冲突停止自动覆盖，保留本地草稿、dirty 和明确冲突提示
- [x] 无效名称或 definition 不提交，修正后可恢复自动保存
- [x] 返回列表前 flush 最新草稿，失败时停留在当前画布
- [x] 完整执行和“从此节点继续”使用 flush 后的最新 revision，保存失败时不创建 Run
- [x] 另存为模板和进入图层编辑器前 flush，不再要求用户先点击保存
- [x] dirty、保存中、失败和冲突状态保留 `beforeunload` 保护，保存成功后不提示
- [x] Pipeline 与模板既有 revision 乐观锁、撤销/重做、运行和另存模板能力无回归
- [x] 前端完整 Vitest、TypeScript、ESLint 和 production build 通过
- [x] 桌面与窄屏浏览器完成自动保存和返回导航验收
