- [x] 桌面端顶部命令栏、`184px` 节点库、中央画布和按需详情栏构成稳定工作台
- [x] 桌面端默认收起右侧详情栏，中央画布获得释放空间
- [x] 节点库按 input、model、control、output 分类并保留完整名称和模态色图标
- [x] 节点库独立滚动，点击项目仍按现有规则添加节点
- [x] 选择节点自动展开详情栏并切换到“配置”
- [x] 顶部“运行记录”展开详情栏并切换到“运行”
- [x] 完整执行和“从此节点继续”成功创建 Run 后展开详情栏、切换到“运行”并选择新 Run
- [x] “配置 / 结果 / 运行”保持既有数据隔离、轮询、取消、重试和下载行为
- [x] 关闭详情栏不清除节点选择、当前标签或 Run 选择
- [x] 顶部“详情”在配置标签下可关闭面板，在结果或运行标签下切回配置
- [x] 点击画布空白处取消节点选择但保留详情标签；配置显示未选择提示，结果与运行继续展示当前 Run
- [x] Pipeline 顶部可访问返回、自动保存状态、撤销、重做、详情、运行记录、另存模板和执行
- [x] 模板顶部不展示运行记录、另存模板和执行
- [x] 模板详情栏保留三个标签，并在结果和运行标签展示既有不可执行空状态
- [x] 长标题和长自动保存状态不会挤出或覆盖命令按钮
- [x] React Flow 缩放、适配视图和交互锁定控件固定在画布底部中央
- [x] 底部工具坞不修改节点坐标、视口持久化或画布平移语义
- [x] 小于 `1024px` 时节点库和详情栏以覆盖式互斥面板呈现
- [x] 窄屏添加节点后节点库自动关闭
- [x] 窄屏详情栏最大宽度不超过视口
- [x] 跨越 `1024px` 时节点浮层状态被清除，详情栏状态和当前标签在停靠与覆盖形态间保留
- [x] `390px` 下返回、状态、撤销、重做、面板入口和执行均可访问且无重叠
- [x] `768px` 下顶部保持单行，紧凑命令只显示图标且名称可访问
- [x] 面板开合和标签切换不触发自动保存
- [x] 节点选择、Run 选择和交互锁定不触发 Pipeline 或模板自动保存
- [x] 名称、描述、节点、连线、位置、尺寸、配置和视口仍执行 `800ms` 防抖自动保存
- [x] 返回、执行、从节点继续、另存模板和打开图层编辑器仍在操作前 `flush()`
- [x] 保存失败、草稿无效或 `409` 冲突仍阻止依赖最新 revision 的命令
- [x] 节点添加、删除、拖拽、缩放、连线校验、运行投影和下载无回归
- [x] 前端定向 Vitest、完整 Vitest、TypeScript、ESLint 和 production build 通过
- [x] 在至少 `1440x900`、`1024x768`、`768x1024` 和 `390x844` 视口完成浏览器截图验收
- [x] 可重复执行的 Playwright 脚本使用固定 fixture、断言控制台无错误并输出约定截图
