- [x] 上游图片节点在当前所选 Run 有成功且可访问的图片结果时，常驻精准编辑按钮可用
- [x] 等待、失败、取消、不可用或无安全地址时按钮保持可见但禁用，且不回退显示本地备用图片
- [x] 点击上游精准编辑按钮后，弹窗展示当前上游结果资产并保持原始宽高比
- [x] 上游框选保存为 `upstream_bbox / upstream_bbox_asset_id`，且绑定当前结果 `asset_id`
- [x] 本地 `asset_id / bbox / bbox_asset_id` 不被上游框选覆盖，断开连线后完整恢复
- [x] 弹窗打开后若上游结果变化，旧草稿不能提交且不会修改节点配置或文本引用
- [x] 切换到不同资产的 Run 后旧框选自动显示为失效，不覆盖在新图片上
- [x] 切换 Run 本身不修改 Pipeline definition；切回匹配资产的 Run 后原框选可恢复
- [x] 上游换图后的失效状态明确提示用户重新框选
- [x] 上游图片节点可与本地文本节点建立符合共同普通图生图下游规则的 BBox 引用
- [x] 上游文本节点、非普通图生图目标、不完整共同下游、重复来源及超过 10 条的引用仍被拒绝
- [x] 有效上游 BBox 按图片输入顺序编译为 `图N<bbox>...</bbox>`
- [x] 实际上游资产与绑定资产不一致时返回 `bbox_reference_asset_changed`，且 Provider 未被调用
- [x] 失效引用不会静默沿用旧坐标、删除引用或降级为无 BBox 提示词
- [x] 下游 `inputHash` 包含有效 BBox、绑定资产 ID 和引用说明，换图或重新框选后不会错误命中旧缓存
- [x] 清除上游框选时同步清除上游绑定和相关文本引用，不影响本地框选
- [x] 模板清除上游 BBox 绑定及相关文本引用，不保护或复制 Run 结果资产
- [x] v1/v2 迁移、API 往返和历史快照读取对新增字段向后兼容
- [x] 既有本地图片精准编辑、BBox 编译、断开恢复、下载和原图预览行为无回归
- [x] 前端完整 Vitest、TypeScript、ESLint 和 production build 通过
- [x] 后端定向测试与完整 pytest 通过
- [x] `1440x900`、`1023x768`、`390x844` Playwright 验收无重叠、裁切、溢出或不可达操作
- [x] 浏览器控制台无新增错误
