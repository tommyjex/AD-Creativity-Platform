- [x] Seedance 2.0、2.0 Fast 和 2.0 Mini 接受 `-1` 或 4 至 15 秒的整数时长。
- [x] Seedance 2.5 接受 `-1` 或 4 至 30 秒的整数时长。
- [x] 自动时长 `-1` 会保存到任务快照、在重试时保留，并以 `duration=-1` 原样传给供应商。
- [x] 前端在模型切换后不会保留超出新范围的时长，后端也会拒绝无效时长请求。
- [x] 参考图片、视频和音频均支持上传后自动选中、从兼容资产库选择和提交前移除。
- [x] 移除参考素材仅移除当前配置引用，不删除资产、对象文件或其他任务引用。
- [x] 生视频提交和失败重试均保存并复用实际模型、时长和三类参考资产 ID。
- [x] 后端相关 pytest 在根目录 `.venv` 中通过。
- [x] 前端相关 Vitest、lint 和 typecheck 通过。
- [x] 浏览器验收确认三种素材选择/移除、时长校验和提交请求正确。
