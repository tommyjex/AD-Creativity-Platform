# Tasks

- [x] Task 0: 支持全模态参考生视频自动时长 `-1`。
  - [x] SubTask 0.1: 扩展后端 schema 与 ModelArk 服务层时长校验，使 `-1` 对四个模型均合法，其余值继续按既有模型范围校验。
  - [x] SubTask 0.2: 更新前端时长输入、模型切换收敛与提示文案，使 `-1` 可输入、可提交且不会被切换模型改写。
  - [x] SubTask 0.3: 补充前后端测试，覆盖 `-1` 创建、重试、供应商透传、前端提交和模型切换。

- [x] Task 1: 扩展全模态生视频时长契约和服务端验证。
  - [x] SubTask 1.1: 将工具生视频请求时长改为整数秒，并集中定义四个模型的最小和最大时长。
  - [x] SubTask 1.2: 在创建和重试生视频任务时验证模型与时长组合，并将合法值映射到 ModelArk 请求和任务输入快照。
  - [x] SubTask 1.3: 补充后端测试，覆盖两个模型族的边界值、超界值、非整数值和重试验证。

- [x] Task 2: 扩展工具参考素材的资产库选择与引用管理。
  - [x] SubTask 2.1: 提供可供工具页选择的兼容公共资产数据，保留既有工具资产上传和任务引用语义。
  - [x] SubTask 2.2: 校验图片、视频和音频参考资产的可访问性与 MIME 类型，确保提交与重试仅使用兼容资产。
  - [x] SubTask 2.3: 补充后端测试，覆盖从资产库引用三种素材、类型不匹配拒绝和重试复用。

- [x] Task 3: 更新全模态参考生视频工作台。
  - [x] SubTask 3.1: 将固定时长下拉框改为带模型范围提示的数值输入，并在切换模型时收敛非法时长。
  - [x] SubTask 3.2: 为图片、视频、音频实现已选素材列表和移除控件，支持上传后自动选中及从资产库选择。
  - [x] SubTask 3.3: 使素材选择器按模态展示兼容资产，并在提交前将当前参考列表准确映射至 API 请求。

- [x] Task 4: 验证全链路行为。
  - [x] SubTask 4.1: 更新前端单元测试，覆盖模型时长边界、模型切换、上传/资产库选择和移除三种参考素材。
  - [x] SubTask 4.2: 在根目录 `.venv` 运行相关 pytest；在前端运行相关 Vitest、lint 和 typecheck。
  - [x] SubTask 4.3: 使用浏览器验证选择资产、移除引用、不同模型时长输入及提交请求。

# Task Dependencies

- Task 2 可与 Task 1 并行。
- Task 3 依赖 Task 1 和 Task 2。
- Task 4 依赖 Task 1、Task 2 和 Task 3。
