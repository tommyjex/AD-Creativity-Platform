- [x] 全模态生成任务按创建时间倒序展示，每页最多 10 条；超过 10 条时可使用分页控件翻页。
- [x] 新提交或重试全模态任务后回到第一页并自动选中该任务；10 条及以下不显示分页控件。
- [x] 每个任务条目有删除入口，删除前需要确认；取消不改变任务列表、页码或选择。
- [x] 删除 API 成功后任务记录从列表和后续查询中消失，删除不存在任务返回 404。
- [x] 删除任务不会删除关联的参考素材、生成结果或其他工具资产。
- [x] 删除选中任务后，选中任务与页码按规格回退；最后一个任务被删除时显示空状态。
- [x] 删除 API 失败时任务列表、页码和选择保持，且显示用户可读错误。
- [x] 后端 `.venv` pytest、前端 Vitest、lint 和 typecheck 通过。
- [x] 浏览器验收确认分页、确认/取消删除、选中回退与播放器预览不回归。
- [x] 当前运行时 OpenAPI 在 `/api/tools/tasks/{task_id}` 路径包含 `DELETE` 方法。
- [x] 对不存在任务执行运行时 `DELETE` 返回 404，而不是 405。
- [x] 工具任务删除后端回归测试和前端删除调用测试通过。
