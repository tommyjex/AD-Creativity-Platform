- [x] AIGC 节点面板可添加“视频人脸打码”节点，旧 `schemaVersion=1` 画布仍可加载。
- [x] 节点只接受一个 `video_asset` 输入并输出一个 `video_asset`，非法或缺失连接会在调用供应商前失败。
- [x] 默认配置为马赛克和中等强度，并可切换高斯模糊及低/中/高强度。
- [x] 节点配置、执行快照和供应商请求均不包含 `face_confidence` 或 `face_box_expand`。
- [x] Gateway 校验公开视频资产、MIME、可访问性、最长 600 秒、25–60 FPS 和最高 4K 限制。
- [x] MediaKit 请求使用现有 API Key、正确端点、白名单字段和稳定 `client_token`，且不泄露密钥。
- [x] 现有工具页人脸打码调用无需 `client_token`，提交、轮询、重试和结果行为不回归。
- [x] MediaKit 轮询正确处理排队、运行、完成、失败、未知状态、非法响应、网络错误和超时。
- [x] 打码结果在临时地址失效前以流式方式转存，不将大型视频完整缓存在内存。
- [x] MP4/MOV 输出资产状态、角色、类型、关联和打码元数据正确，失败时无空资产或残留引用。
- [x] 人脸打码使用独立并发、轮询、任务超时和转存限制，不占用 Seedance 或画质增强槽位。
- [x] 参数与输入变化会使缓存失效；相同 input hash 且资产可用时复用成功结果。
- [x] 重试、取消、Worker 租约、晚到结果保护、从节点继续和独立分支收敛符合现有 AIGC 语义。
- [x] 仅包含视频人脸打码可执行节点的有效 Pipeline，其“执行”按钮可用并能创建 Run。
- [x] 配置面板紧凑展示方式和强度，节点摘要清晰且布局无溢出。
- [x] 结果面板和视频输出节点可播放、放大、全屏、下载打码结果，并保持视频原始宽高比。
- [x] 运行日志展示状态、时间、耗时、Attempt、供应商追踪标识和脱敏失败原因。
- [x] 后端相关 pytest 在根目录 `.venv` 环境通过。
- [x] 前端相关 Vitest、typecheck、lint 和 production build 通过。
- [x] 浏览器 Mock 验收覆盖添加、连线、配置、保存重载、执行、失败重试、播放、放大、全屏和下载。
- [x] 自动化测试和浏览器验收未调用真实计费的 MediaKit 人脸打码 API，测试结束后无残留浏览器进程。
