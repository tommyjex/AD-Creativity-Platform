- [x] AIGC 节点面板可添加视频输入、音频输入、生视频和视频输出节点，旧 `schemaVersion=1` 画布仍可无迁移加载。
- [x] 前后端同构契约包含 `video_asset`、`audio_asset` 端口和 `VIDEO_GENERATION` task type。
- [x] 视频输入仅接受视频资产，音频输入仅接受音频资产，并支持本地上传和资产库选择。
- [x] 视频输入可等比播放和放大预览；音频输入可播放；两者显示可获得的文件及媒体信息。
- [x] 模板创建、更新和实例化会清除图片、视频、音频输入节点的具体 `asset_id`。
- [x] 生视频节点支持文生视频、首帧、首尾帧、全模态参考四种互斥模式。
- [x] 文生视频要求非空提示词且不接受媒体；首帧要求一张首帧图；首尾帧要求首尾各一张图。
- [x] 全模态模式至少包含提示词或一种参考素材，并禁止与首帧/尾帧端口混用。
- [x] Seedance 2.5 全模态模式最多接受 30 张图片、10 个视频、10 个音频，并允许仅音频输入。
- [x] Seedance 2.0、2.0 Fast、2.0 Mini 全模态模式最多接受 9 张图片、3 个视频、3 个音频，且拒绝仅音频输入。
- [x] 第一个超出模型数量上限的连接在前端被拒绝，后端也拒绝绕过前端的非法 definition。
- [x] Seedance 2.5 分辨率仅允许 480p、720p、1080p。
- [x] Seedance 2.0 分辨率仅允许 480p、720p、1080p、4k。
- [x] Seedance 2.0 Fast 和 Mini 分辨率仅允许 480p、720p。
- [x] 四个模型默认分辨率为 720p，模型切换后非法分辨率收敛为 720p。
- [x] 四个模型均支持 16:9、4:3、1:1、3:4、9:16、21:9、adaptive 宽高比。
- [x] Seedance 2.5 时长接受 `-1` 或 4-30 秒整数，2.0 系列接受 `-1` 或 4-15 秒整数。
- [x] 生视频节点默认时长为 `-1`，模型切换后非法时长收敛为 `-1`。
- [x] 用户可选择生成有声或无声视频，`generate_audio` 正确进入供应商请求、任务快照、缓存 Hash 和结果元数据。
- [x] 多参考素材按稳定连接顺序编号并传给供应商；素材重排会使缓存失效。
- [x] Seedance 共享能力矩阵由工具页与 AIGC 共用，现有独立视频工具行为通过回归测试。
- [x] AIGC 复用领域中立的 Seedance 创建、轮询、解析和错误脱敏链路，不创建 `ToolTask`。
- [x] AIGC 视频任务使用自身 Run、RunNode、Task 状态，并具有独立视频并发限制与超时配置。
- [x] 执行前重新校验输入资产状态、MIME 和可访问性，并按 slot/ordinal 记录输入资产引用。
- [x] 视频结果转存到现有对象存储和资产库，并可追溯 pipeline、run、node、task 与模型参数。
- [x] 视频输出节点和结果面板可播放可用结果，固定媒体区保持原始宽高比且不裁切或拉伸。
- [x] 视频下载使用受控资产地址，文件名符合“节点标题-序号.扩展名”。
- [x] 失败、超时、自动重试、取消和晚到结果保护遵循现有 AIGC 调度语义，独立分支不被错误阻塞。
- [x] 全量执行、从节点执行、失败重试和同 Pipeline 缓存复用均覆盖生视频节点。
- [x] 后端完整测试在仓库根目录 `.venv` 环境中通过。
- [x] 前端完整 Vitest、lint、typecheck 和 production build 通过。
- [x] 浏览器验收覆盖四类节点、四种模式、四个模型、素材限制、保存重载、执行、预览和下载。
- [x] 桌面与窄屏下节点、配置面板和播放器无不合理重叠或溢出。
