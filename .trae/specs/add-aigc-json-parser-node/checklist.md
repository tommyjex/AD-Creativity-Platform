- [x] 节点面板“控制”分组包含“JSON 解析器”，默认 JSONPath 为 `$.items`
- [x] JSON 解析器只有一个必填文本输入，拒绝第二条入边和非文本入边
- [x] `items` 输出仅允许系统生成连线，最多物化 20 个受管文本节点
- [x] 前后端节点类型、配置 schema、端口 ID、类型和连接上限保持一致
- [x] 保存期和运行期使用同一后端 JSONPath 语义并拒绝非法表达式
- [x] 纯 JSON 和仅包裹一层 Markdown JSON 代码块的输入可解析
- [x] 代码块外说明文字、空输入和非法 JSON 返回 `json_parser_invalid_json`
- [x] JSONPath 无匹配、多匹配和非数组结果分别返回稳定错误码
- [x] 空数组成功，20 项成功，21 项整体失败且不产生部分节点
- [x] 字符串 item 原样输出，其他 JSON 类型使用稳定紧凑 JSON 文本
- [x] 混合 JSON 类型、Unicode、键排序和 item 摘要具有确定性
- [x] 解析器不调用外部模型，但创建 RunNode 和 Task Attempt
- [x] 解析结果使用结构化有序 item 列表，而非前端二次解析的拼接文本
- [x] 首次成功后，页面即使关闭，后端仍原子创建文本节点和系统连线
- [x] 自动节点标题为“JSON 项 N”，并保存解析器 ID、item 索引和来源 Run ID
- [x] 自动节点位于解析器右侧，布局稳定且不与现有节点明显重叠
- [x] Pipeline definition 与 revision 在一次物化中原子更新且只递增一次
- [x] 任一物化步骤失败时事务回滚，不保留部分节点或边
- [x] 来源解析器在运行期间删除或变更类型时不创建幽灵节点
- [x] 首次 Run 的 definition snapshot 和 RunNode 集合保持不可变
- [x] 首次生成节点立即可查看，但只从后续 Run 开始参与 DAG
- [x] 后续 Run 按 item 索引投影受管文本并正常释放其下游
- [x] 重跑时相同索引复用节点 ID、位置、尺寸和额外下游连线
- [x] item 增加时只补建缺少索引的受管节点
- [x] item 减少时删除无额外下游的多余节点
- [x] item 减少时保留有额外下游的节点，显示“无对应 item”并阻塞本次后代
- [x] 不存在当前 item 时不得复用上一 Run 的旧文本
- [x] 删除系统连线会把受管节点转为普通本地文本节点并保留当前内容
- [x] 删除受管节点后，下次运行可按缺失索引重建
- [x] 后端物化只修改来源解析器拥有的节点和边，不覆盖其他用户编辑
- [x] 系统 revision 更新时，clean 草稿刷新完整 definition
- [x] 系统 revision 更新时，dirty 草稿安全合并受管增量并保留用户修改
- [x] 物化与自动保存的两种提交顺序均不会静默丢失修改
- [x] 节点卡片和运行面板展示解析状态、item 数量、错误码和中文说明
- [x] 受管文本节点沿用文本蓝色、只读上游展示和可识别的来源名称
- [x] JSON 解析器纳入 input hash、缓存、取消、重试和失败传播
- [x] 一个解析器失败不会影响不相连的其他流程
- [x] 模板保留 JSONPath，但清除来源 Run 和不可复用的运行内容
- [x] 错误响应、日志和快照不记录完整上游 JSON、密钥或堆栈
- [x] MemoryRepository 与 MySQLRepository 的物化、revision 和回滚行为一致
- [x] 后端 JSON、DAG、仓储、执行器定向测试及完整回归通过
- [x] 前端 lint、typecheck、完整 Vitest 和 production build 通过
- [x] Playwright 验证首次生成、重跑增减、位置保留和下游连线保护
- [x] 宽屏、1024px 和移动端布局无文字溢出、控件重叠或画布异常
