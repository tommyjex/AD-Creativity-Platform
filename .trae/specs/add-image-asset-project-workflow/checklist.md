- [x] Project schema 仅接受 `video_ad` / `image_asset`，旧请求与历史项目默认 `video_ad`。
- [x] 项目类型创建后不可修改，历史迁移先幂等回填再建立非空约束。
- [x] 图片项目必须选择 `ecommerce_main` / `poster`，视频项目不保存图片用途。
- [x] Brief 字段矩阵得到执行：图片项目必填商品名/卖点/受众并拒绝视频时长，视频项目要求时长且无图片用途。
- [x] 新建/编辑平台只提交稳定内容或电商平台值，历史非空自定义平台值仍可读取。
- [x] 新旧数据库迁移幂等，项目与 Brief 现有数据无丢失。
- [x] 内存与 SQLAlchemy 仓储对项目类型、图片用途和图片领域对象行为一致。
- [x] 新建项目表单可选择广告视频、图片素材、电商主图和海报，图片模式不要求视频时长。
- [x] 项目列表和详情显示正确类型标签，搜索与软删除同时适用于视频和图片项目。
- [x] 视频项目继续进入现有六阶段工作流且无行为回归。
- [x] 图片项目进入独立图片工作台，不显示故事、角色、剧本、分镜或视频合成操作。
- [x] 图片 Brief 保存并展示商品名、卖点、目标受众、广告需求、风格、比例和目标语言。
- [x] 图片提示词支持保存、版本历史和最新版本回显。
- [x] 中文 300 字、英文 600 词官方建议具有清晰计数与警告，用户确认后仍可保存。
- [x] 生成任务冻结提示词版本、全文和模型参数，排队后切换提示词不改变任务输入。
- [x] Brief 相关字段变化只使图片提示词及其下游结果 stale，历史图片不删除。
- [x] 失效状态复用现有 Status.STALE：项目 image_prompt_status 与派生 Asset.status 更新，不新增 freshness 字段且不修改不可变 ImagePromptVersion。
- [x] stale 图片仍可历史预览但不可设为当前，重新生成产生新的 succeeded 资产。
- [x] Seedream 文生图使用正确模型、单图非流式参数、2K PNG 默认值和无水印。
- [x] 图片生成仅允许 Seedream 5.0 Pro 支持的 `1K`、`1.5K`、`2K` 尺寸档位。
- [x] 画面比例以规范化 Prompt 约束传递，不被误用为 Seedream `size`。
- [x] 远程生成结果经过下载校验、TOS 上传后才写入成功资产。
- [x] asset_role 新库默认 public，历史资产幂等回填 public 后普通资产查询仍完整。
- [x] 模型、下载、上传或数据库失败不会留下前端可见的半成品。
- [x] 多次生成与编辑创建不可变版本，并正确记录 operation、source_asset_id 和 prompt_version。
- [x] 用户可以将任意成功图片版本设为当前成品，其他版本继续保留。
- [x] 普通图片编辑使用单张源图片和编辑提示词调用 Seedream 图生图。
- [x] point 坐标被正确转换、校验并组装为 0–999 的 `<point>` 标签。
- [x] bbox 坐标被正确转换、校验并组装为有序的 0–999 `<bbox>` 标签。
- [x] 坐标基于图片实际内容区计算，使用 round/clamp 规则且不受预览留白影响。
- [x] 后端只接受结构化标注数据，不信任客户端直接拼接的坐标标签。
- [x] 图层拆分只接受一张符合格式、大小、像素和宽高比要求的 PNG/JPEG。
- [x] 图层拆分请求使用 `layer_decomposition=true` 和合法 size，未发送 stream/tools/sequential 参数。
- [x] 图层拆分第一项解析为底图，其余图层按 z_index 保存并包含绝对/归一化边界框、名称和描述。
- [x] 缺失、重复、不连续 z_index 或非法 absolute/normalized bbox 会使整个拆分失败。
- [x] absolute bbox 使用右/下排他边界，允许 x2=width、y2=height 且合成尺寸为坐标差值。
- [x] 图层拆分最多接受 1 张底图和 16 个图层，任何子项失败时整体回滚并清理临时对象。
- [x] TOS 上传成功但数据库事务失败时，本次临时对象被补偿删除且集合不可见。
- [x] 图层对象使用最终私有键；数据库成功后永久保留，失败时补偿删除，不存在未定义的临时键晋升。
- [x] 图层集合与各图层的源资产、内部资产、边界框、顺序、可见性和变换均结构化持久化。
- [x] 内部底图和拆分图层不会作为普通成品卡片出现在资产库。
- [x] 普通资产列表、搜索、详情和内容接口均拒绝内部资产，只有所属图层集合接口可返回临时访问 URL。
- [x] 图层编辑器能按底图坐标系和 z_index 还原原始构图。
- [x] 图层编辑器支持选择、显隐、移动、等比缩放和调整顺序，底图不可删除。
- [x] 首期不支持删除、旋转、裁剪、调色或替换图层，变换使用底图像素坐标和合法缩放范围。
- [x] 图层状态保存后刷新可恢复，并处理并发版本冲突。
- [x] 图层合成按可见性、坐标、变换和顺序生成像素正确的 PNG。
- [x] 图层导出只接受已保存的集合 revision，过期 revision 返回冲突且不生成资产。
- [x] 图层合成失败不会覆盖当前成品或产生成功资产。
- [x] 资产库展示文生图、图片编辑和图层合成成品，并显示完整来源元数据。
- [x] 图片项目软删除后，后端图片、图层和存储对象保留，前端项目与资产查询不可见。
- [x] 图片生成、编辑、拆分和合成任务状态可观察、可重试，并阻止重复运行任务。
- [x] 相同输入的重复提交返回现有运行任务，失败重试创建带 retry_of_task_id 的新 attempt。
- [x] 失败重试复制原任务冻结输入、参数和 input_hash，不读取最新提示词、资产或图层 revision。
- [x] 后端全量测试使用项目根目录 `.venv` 执行通过。
- [x] 前端全量测试、TypeScript、ESLint 和构建执行通过。
- [x] 真实 Seedream 文生图、图生图、point/bbox 编辑和图层拆分联调通过。
- [x] 浏览器桌面与移动端验证图片工作流完整、画布非空且无控件重叠或水平溢出。
- [x] 资产库可预览、下载和筛选图片成品，且不泄露内部图层。
- [x] 规格中引用的 Seedream 参数与官方图片生成、交互编辑和图层拆分文档一致。
