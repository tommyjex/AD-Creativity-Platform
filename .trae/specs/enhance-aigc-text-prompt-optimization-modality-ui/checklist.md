- [ ] 文本节点“优化提示词”打开弹窗而非立即请求
- [ ] 弹窗包含当前文本摘要、可选优化方向、目标模型、取消和提交
- [ ] 优化方向最多 2000 个 Unicode code point 且不持久化
- [ ] 目标选择只列出直接连接至 `prompt` 的 LLM、生图和生视频模型
- [ ] 多个同类型下游按节点显示名称分别列出
- [ ] 间接下游、控制节点和非模型节点不进入目标列表
- [ ] 无支持的直接下游时优化入口禁用且不默认文生图
- [ ] 文本、覆盖、所选 Run、优化方向、BBox 引用、目标、连线或配置变化时过期结果不写回
- [ ] 请求期间切换到文本相同的其他 Run 仍使响应失效
- [ ] 目标变化导致结果丢弃时显示明确提示
- [ ] 请求期间取消、关闭和 Escape 禁用，页面卸载或节点删除后响应不写回
- [ ] LLM 优化明确角色、上下文、任务、规则和输出约束
- [ ] LLM 优化不要求输出隐藏思维过程，不编造事实或样例
- [ ] LLM 优化只返回一个可直接使用的提示词
- [ ] 文生图优化遵循主体、行为、环境、用途和必要美学原则
- [ ] 图片编辑明确编辑对象、动作和保持不变内容
- [ ] 参考图生图明确素材对象、用途、保留特征和多图关系
- [ ] 图生图普通参考素材按 definition edge 顺序稳定传入优化上下文
- [ ] 生图优化保持文字引号、硬约束和 BBox 结构
- [ ] BBox 引用 ID、顺序、数量、坐标和安全 token 不变
- [ ] BBox 顺序按文本节点配置数组保持，用户仍不能手工输入坐标标签
- [ ] Provider 篡改 BBox token、坐标或引用说明数量时整次响应被拒绝
- [ ] 非生图目标不修改 BBox 引用说明
- [ ] 生视频优化读取生成模式、时长、画幅和音频配置
- [ ] 生视频优化按目标时长组织连续镜头或时间轴
- [ ] 生视频优化明确参考图片、视频、音频的编号和用途
- [ ] 生视频端口映射为明确角色并按固定端口语义及同端口 edge 顺序稳定编号
- [ ] 生视频优化覆盖动作、景别、机位、运镜、声音和负向约束
- [ ] 生视频优化不引用不存在的素材或生成模式
- [ ] 优化方向作为软约束，不覆盖原文品牌、产品、文字、数量、颜色、画幅、时长和否定条件
- [ ] 后端从原文本和每条 BBox 说明独立提取引号、URL、反引号、宽高比、带单位数字和 BBox token
- [ ] 字面量提取遵循最长匹配、类别优先级、原 code point、顺序及重复计数规则
- [ ] Provider 输出缺失或改写任一受保护字面量时整次响应被拒绝
- [ ] 固定回归语料覆盖品牌、产品、颜色和否定语义保持
- [ ] 优化 API 权威校验目标类型、配置组合、长度、引用和响应结构
- [ ] 优化响应固定返回单个文本和引用说明数组，生图结果原子写回
- [ ] LLM 和生视频优化响应的引用说明数组必须为空
- [ ] 缺少明确目标的旧版优化请求返回 `invalid_input`
- [ ] 优化 API 不保存 Pipeline、不创建 Run/Task、不调用图片或视频生成
- [ ] 优化中弹窗控件禁用且不会重复提交
- [ ] 优化失败保留原内容并显示脱敏错误
- [ ] 优化成功作为一次可撤销操作且不自动保存
- [ ] 无变化结果不增加历史或改变 dirty
- [ ] `TextConfig` 前后端均支持可空 `upstream_text_override`
- [ ] 有上游无覆盖时有效文本来自所选 Run 上游结果
- [ ] 有上游有覆盖时有效文本来自覆盖值
- [ ] `null` 表示无覆盖，空字符串和纯空白表示明确的空覆盖
- [ ] 编辑或优化上游文本只修改当前节点覆盖值
- [ ] 上游节点、当前/历史 RunNode 和历史 snapshot 不被修改
- [ ] “恢复上游文本”清除覆盖并显示当前界面所选 Run 的上游结果
- [ ] 切换历史 Run 不修改覆盖值，清除后按所选 Run 投影文本
- [ ] 所选 Run 等待、失败或 unavailable 时恢复操作显示对应状态而不冒充其他文本
- [ ] 上游重跑不会自动覆盖或清除当前覆盖值
- [ ] 断开入边后使用本地 `text`，重连后原覆盖重新生效
- [ ] 覆盖值变化使消费它的下游 `inputHash` 变化
- [ ] 覆盖值不改变上游拓扑依赖，失败、取消、超时和 unavailable 仍阻塞后代
- [ ] 四类模态节点卡片不显示“本地 / 上游”标签
- [ ] 四类模态节点详情栏不显示来源标签或冗余来源说明
- [ ] 本地模态节点的原编辑能力保持不变
- [ ] 上游文本可编辑并提供恢复操作
- [ ] 上游图片、视频、音频禁用上传和资产库选择
- [ ] 上游图片的精准框选等本地资产编辑继续禁用
- [ ] 断开上游后本地媒体资产和 BBox 自动恢复
- [ ] 图片和视频模态节点标题栏不显示下载按钮
- [ ] 可用图片和视频仅在详情栏提供下载
- [ ] 不安全、等待、失败和 unavailable 图片/视频不能下载
- [ ] 下载沿用现有受控 URL 判定且 `available=false` 明确视为 unavailable
- [ ] 音频节点和结果面板现有下载行为无回归
- [ ] 文本蓝、图片绿、视频橙、音频玫红及节点编号无回归
- [ ] LLM、生图、生视频、图层和多轨剪辑执行无回归
- [ ] 后端定向测试和完整 pytest 通过
- [ ] 前端定向测试、完整 Vitest、TypeScript、ESLint 和生产构建通过
- [ ] Mock 浏览器验收覆盖三类优化、上游覆盖、恢复和下载
- [ ] `1440x900`、`1023x768`、`390x844` 无标签残留、重叠、溢出或不可达操作
- [ ] 浏览器控制台无新增错误且未调用真实计费接口
