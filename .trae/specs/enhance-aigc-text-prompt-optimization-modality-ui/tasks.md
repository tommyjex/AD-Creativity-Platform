# Tasks

- [x] Task 1: 扩展文本节点与提示词优化契约。
  - [x] 在前后端 `TextConfig` 增加可空 `upstream_text_override`，保持 v1/v2 读取兼容。
  - [x] 将优化模式扩展为 `llm / text_to_image / image_to_image / video_generation`，定义目标节点白名单配置、优化方向和模态上下文。
  - [x] 定义固定响应结构和生图引用说明原子写回规则。
  - [x] 定义后端规范字面量提取器、逐字段响应保留校验，以及视频素材端口到角色的稳定映射。
  - [x] 增加长度、非法目标、配置不匹配、引用数量及旧版无目标请求明确拒绝测试。

- [x] Task 2: 实现三类目标专属优化策略。
  - [x] 按官方 LLM 指南实现角色、上下文、任务、规则、示例和输出约束策略。
  - [x] 复用并收紧 Seedream 文生图、图片编辑、参考图和 BBox 优化策略。
  - [x] 按 Seedance 指南实现模式、时长、镜头、素材映射、动作、运镜和声音策略。
  - [x] 实现优化方向的软约束合并和原始硬约束保护。
  - [x] 使用固定品牌、产品、颜色、否定语义和字面量语料覆盖三类策略、冲突输出拒绝、Provider 失败和无持久化副作用。

- [x] Task 3: 接入上游文本覆盖的有效值与执行语义。
  - [x] 前后端统一解析 `override > 上游结果`，无入边时继续使用本地 `text`。
  - [x] 将覆盖值纳入下游参数快照和 `inputHash`，并保持上游拓扑依赖及失败传播。
  - [x] 实现编辑、清除、断开/重连、上游重跑、历史 Run、缓存命中/失效和模板行为。
  - [x] 增加 DAG、执行器、缓存、迁移和 API 往返测试。

- [x] Task 4: 实现目标感知优化弹窗与并发保护。
  - [x] 从当前未保存 definition 推导受支持的直接下游候选，并使用显示名称逐节点列出。
  - [x] 实现优化方向、目标选择、文本摘要、提交、取消、loading、错误和重试。
  - [x] 根据本地文本或上游有效文本构造请求；仅生图目标携带并更新 BBox 引用说明。
  - [x] 捕获文本、覆盖、所选 Run、方向、目标配置、连线和引用快照，丢弃过期响应并显示具体变化提示。
  - [x] 请求期间禁用取消、关闭和 Escape；页面卸载或节点删除后忽略响应。
  - [x] 将成功结果作为单次可撤销操作写入本地文本或上游覆盖，无变化不污染历史。

- [x] Task 5: 改造模态节点详情与来源显示。
  - [x] 移除四类模态节点卡片和详情栏中的可见“本地 / 上游”标签与来源说明。
  - [x] 上游文本使用可编辑有效文本，增加“恢复上游文本”，并处理等待、失败和 unavailable。
  - [x] 上游图片、视频、音频禁用本地上传、资产库选择及其他本地资产编辑。
  - [x] 保持断开入边后本地文本、资产和 BBox 恢复。
  - [x] 更新可访问名称、窄屏布局和相关组件测试。

- [x] Task 6: 调整图片与视频下载入口。
  - [x] 从图片、视频模态节点标题栏移除下载按钮和关联点击处理。
  - [x] 保证详情栏对可用图片、视频提供下载，对等待、失败、不安全 URL 和 unavailable 正确禁用。
  - [x] 保持音频节点下载行为及结果面板下载不变。
  - [x] 增加标题栏、详情栏、事件冒泡、安全 URL 和稳定文件名回归测试。

- [x] Task 7: 完成全量与浏览器验收。
  - [x] 运行后端契约、优化服务、DAG、执行器、缓存、迁移及完整 pytest。
  - [x] 运行前端 Store、弹窗、节点、详情、下载定向测试及完整 Vitest、TypeScript、ESLint 和生产构建。
  - [x] 使用 Mock Provider 验证 LLM、生图、生视频目标选择、优化方向、替换、撤销、失败和过期响应。
  - [x] 验证上游文本编辑、恢复、重跑保留、断开/重连及上游失败阻塞。
  - [x] 在 `1440x900`、`1023x768`、`390x844` 验证标签移除、媒体控件禁用、详情下载和无重叠。
  - [x] 审计浏览器网络，确认验收未调用真实计费优化或生成接口。

# Task Dependencies

- Task 2、Task 3 依赖 Task 1，可并行。
- Task 4 依赖 Task 1、Task 2、Task 3。
- Task 5 依赖 Task 3，可与 Task 2 并行。
- Task 6 与 Task 1 至 Task 5 无硬依赖，可并行。
- Task 7 依赖 Task 1 至 Task 6。
