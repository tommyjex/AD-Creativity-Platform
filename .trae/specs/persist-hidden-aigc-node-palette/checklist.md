- [x] 桌面 Pipeline 和模板画布在无偏好时默认隐藏节点库
- [x] 节点库隐藏时画布左上角提供可访问的“打开节点库”按钮
- [x] 节点库显示时标题栏提供可访问的“隐藏节点库”按钮
- [x] 桌面节点库显示后保持 `184px` 宽度、分类、完整名称、独立滚动和节点添加能力
- [x] 显示或隐藏后 `localStorage` 使用版本化键保存严格布尔字符串
- [x] 刷新当前画布及进入其他 Pipeline 或模板时恢复同一浏览器偏好
- [x] 偏好缺失、非法或 localStorage 访问失败时安全回退为隐藏
- [x] 隐藏节点库不清除节点选择、Inspector 标签、Run 选择或画布视口
- [x] 桌面节点库显示时添加节点后保持显示
- [x] 小于 `1024px` 时节点库保持 `240px` 临时覆盖面板并与 Inspector 互斥
- [x] 窄屏添加节点后面板自动关闭且不修改桌面偏好
- [x] 桌面切换到窄屏不自动打开节点面板，返回桌面后恢复持久化偏好
- [x] 节点库显隐和偏好恢复不触发 Pipeline 或模板自动保存
- [x] 节点、连线、运行、撤销重做、右键添加和详情栏行为无回归
- [x] 显隐按钮使用 Lucide 图标、`aria-label`、`title` 及可识别的状态语义
- [x] `1440x900`、`1024x768`、`768x1024` 和 `390x844` 下无不可用重叠或文本溢出
- [x] AIGC 编辑器定向测试、完整前端测试、TypeScript、ESLint 和 production build 通过
- [x] Playwright 验收覆盖默认隐藏、显示、隐藏、刷新恢复、断点切换并输出截图
