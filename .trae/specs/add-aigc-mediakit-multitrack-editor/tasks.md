# Tasks

- [x] Task 1: 建立多轨剪辑节点与剪辑工程契约。
  - [x] 定义 `multi_track_edit` 节点、Task 类型、四类多值输入和视频输出。
  - [x] 定义画布、输出、轨道、五类元素及首版滤镜的前后端强类型。
  - [x] 实现纯函数规范化和校验，允许保存草稿但提供执行级错误。
  - [x] 添加数量、时间、重叠、裁切、倍速、音量、淡入淡出、Transform、字幕和转场边界测试。

- [x] Task 2: 实现 MediaKit 多轨剪辑客户端。
  - [x] 新增隔离客户端，提交 `multi-track-edit`、轮询统一任务接口并解析结果。
  - [x] 将规范工程白名单化为 `canvas / track / output`，解析稳定来源但不持久化 URL。
  - [x] 实现稳定 `client_token`、状态归一化、响应结构校验和脱敏错误。
  - [x] 使用 Mock Transport 覆盖提交、运行、成功、失败、未知状态、超时和密钥不泄露。

- [x] Task 3: 接入 AIGC DAG、执行器和缓存。
  - [x] 解析视频、图片、音频和文本多值输入，并校验元素来源属于直接入边。
  - [x] 从当前 RunNode 解析文本/资产，生成参数快照、输入引用和稳定 `inputHash`。
  - [x] 接入独立并发、轮询、超时、取消、租约、重试、晚到结果保护和从节点继续。
  - [x] 添加来源缺失、类型错误、缓存命中/失效、冻结重试和独立分支测试。

- [x] Task 4: 流式转存并登记多轨成片。
  - [x] 复用远程视频流式转存能力，校验临时 URL、MIME、大小和视频内容。
  - [x] 创建 AIGC 视频输出资产和所有媒体/SRT 输入追溯关系。
  - [x] 保存白名单化 MediaKit、轨道、元素、画布、时长、分辨率和帧率元数据。
  - [x] 覆盖 URL 过期、下载中断、类型错误、大小超限、存储失败和数据库回滚。

- [x] Task 5: 实现画布节点和全屏路由。
  - [x] 在节点面板、Store、React Flow 卡片和执行白名单中接入多轨剪辑节点。
  - [x] 卡片展示轨道数、元素数、时长、画布、帧率、保存与运行状态。
  - [x] 新增 `/timeline` 加载页、错误页和全屏 AppShell 规则。
  - [x] 实现 revision 安全的数据加载、保存、放弃修改和保存后从节点执行。

- [x] Task 6: 实现时间线状态模型与编辑操作。
  - [x] 实现轨道/元素增删、重命名、排序、隐藏、静音和来源绑定。
  - [x] 实现拖拽、裁切、分割、吸附、播放头、缩放及整数毫秒规范化。
  - [x] 实现撤销/重做、dirty、保存状态、来源缺失和执行级校验汇总。
  - [x] 添加 reducer/Store 测试，覆盖边界拖拽、重叠、撤销重做和冲突恢复。

- [x] Task 7: 实现全屏多轨编辑器 UI。
  - [x] 实现顶栏、预览区、轨道头、时间标尺、片段、播放头和属性检查器。
  - [x] 实现视频/图片/文字结构化预览及“最终效果以合成为准”状态。
  - [x] 实现 Transform、裁切、倍速、音量、淡入淡出、转场、文字样式和画布/输出控件。
  - [x] 实现 SRT 上传、字幕轨道、资产保护和模板净化交互。
  - [x] 实现 `<1024px` 抽屉与横向时间线，确保文本、控件和媒体不重叠。

- [x] Task 8: 接入运行结果与日志。
  - [x] 在节点和结果面板播放、全屏、下载多轨成片。
  - [x] 展示轨道数、元素数、分辨率、帧率、时长、状态、耗时、Attempt 和供应商追踪。
  - [x] 展示 validate/submit/poll/transfer/persistence 脱敏错误和缓存复用状态。
  - [x] 添加成功、失败、历史 Run、unavailable 资产和文件名测试。

- [x] Task 9: 完成回归与非计费验收。
  - [x] 运行后端定向测试和完整 pytest。
  - [x] 运行前端定向测试、完整 Vitest、TypeScript、ESLint 和生产构建。
  - [x] 使用 Mock MediaKit 验证创建、连接、全屏编辑、保存、执行、重试、缓存和结果下载。
  - [x] 在 `1440x900`、`1023x768` 和 `390x844` 验证布局、拖拽、裁切、分割、检查器和无溢出。
  - [x] 审计网络请求，确认自动化未调用真实 MediaKit 计费接口。

- [x] Task 10: 修复最终 Checklist 未通过项。
  - [x] 在前端可获得源媒体时长时即时校验 `source_trim`，并在后端执行前使用解析后的资产时长权威拒绝越界裁切；补充视频、音频和不调用 MediaKit 的边界测试。
  - [x] 执行前检查展示全部错误，不截断为前 6 条，并显示可定位的轨道 ID、元素 ID 与字段路径；补充超过 6 条错误及定位信息的组件测试。
  - [x] 在内存库和 MySQL 库的 Pipeline 资产引用收集中纳入所有多轨字幕 `asset_id`，使用不会冲突的稳定 slot；补充保存后禁止删除、移除引用后允许删除及模板净化清除 ID 的双仓储测试。

- [x] Task 11: 修复多轨倍速为零时的后端校验异常。
  - [x] 避免 `speed=0` 在裁切、倍速与目标时长关系校验中触发除零，返回可定位的 `invalid_speed`，并补充零倍速及非法倍速不调用 MediaKit 的回归测试。

# Task Dependencies

- Task 2、Task 5 依赖 Task 1，可并行。
- Task 3 依赖 Task 1、Task 2。
- Task 4 依赖 Task 2，可与 Task 3 并行。
- Task 6 依赖 Task 1、Task 5。
- Task 7 依赖 Task 5、Task 6，可与 Task 3、Task 4 并行。
- Task 8 依赖 Task 3、Task 4、Task 5、Task 7。
- Task 9 依赖 Task 1 至 Task 8。
- Task 10 依赖 Task 9 的最终 Checklist 验证结果。
