- [x] 节点面板可添加“多轨剪辑”节点
- [x] 节点具有视频、图片、音频、文本多值输入和视频输出
- [x] 各输入连接上限、类型、自环和环路校验正确
- [x] 时间线媒体元素只绑定直接上游稳定节点引用，不保存 URL
- [x] 文字支持上游文本和内联文本，SRT 支持节点内上传
- [x] 删除入边后元素保留为来源缺失草稿，保存允许且执行禁止
- [x] 工程最多 20 轨、200 元素、10 条字幕轨
- [x] 轨道顺序与 MediaKit 自下而上渲染层级一致
- [x] 视频、图片、音频、文字和字幕元素契约完整
- [x] 时间和持续时间统一保存为整数毫秒
- [x] 同轨重叠只允许合法相邻视频转场
- [x] 裁切、倍速、目标时长关系校验正确
- [x] Transform、画布尺寸和 RGBA 背景色校验正确
- [x] 音量、淡入淡出和转场边界校验正确
- [x] 字幕资产类型、可用性、独占轨道及数量校验正确
- [x] 不完整时间线可保存，全部执行错误清晰可定位
- [x] 后端执行前权威校验非法时间线且不调用 MediaKit
- [x] MediaKit 请求使用正确端点、Bearer 鉴权和稳定 client_token
- [x] 请求只包含白名单化 canvas、track、output 和 client_token
- [x] 排队、运行、完成、失败、未知状态、非法响应和超时处理正确
- [x] 错误不泄露 API Key、完整响应和签名 URL
- [x] 多轨剪辑使用独立并发、轮询、超时和转存配置
- [x] 参数快照冻结规范化工程，不包含临时 URL
- [x] inputHash 包含工程、有效上游摘要和字幕资产摘要
- [x] 时间线或输入变化使缓存失效，相同可用结果可复用
- [x] 重试、取消、租约、晚到结果和从节点继续符合现有语义
- [x] 输出视频在临时 URL 失效前流式转存
- [x] 输出资产、输入追溯和白名单元数据正确
- [x] 转存或持久化失败无成功资产、缓存或孤立对象
- [x] 节点卡片展示轨道、元素、时长、画布、帧率和运行状态
- [x] `/timeline` 路由隐藏全局导航并使用完整 `100dvh`
- [x] 编辑器顶栏支持返回、保存状态、撤销、重做、放弃和执行
- [x] 预览、检查器、轨道头、标尺、片段和播放头布局完整
- [x] 轨道增删、重命名、排序、隐藏和静音可用
- [x] 片段拖拽、裁切、分割、吸附和缩放可用
- [x] 检查器精确编辑所有首版时间与效果参数
- [x] 浏览器结构化预览明确不冒充最终 MediaKit 渲染
- [x] SRT 上传后纳入 Pipeline 资产保护，模板净化时清除
- [x] 保存使用 Pipeline revision，冲突不会覆盖远端
- [x] 执行剪辑先保存，再从当前节点发起 Run
- [x] 节点和结果面板可播放、全屏、下载成片
- [x] 日志展示状态、耗时、Attempt、供应商追踪和脱敏错误
- [x] 现有统一模态节点、视频生成、增强、打码和图层流程无回归
- [x] 后端定向测试与完整 pytest 通过
- [x] 前端定向测试、完整 Vitest、TypeScript、ESLint 和构建通过
- [x] Mock 浏览器验收覆盖保存、执行、失败重试、缓存和结果
- [x] `1440x900`、`1023x768`、`390x844` 无重叠、溢出或不可达操作
- [x] 自动化和浏览器验收未调用真实计费 MediaKit 接口
