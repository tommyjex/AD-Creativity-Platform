- [x] 资产库来源筛选包含“全部资产 / 项目资产 / 工具资产 / AIGC工作台”
- [x] `source=aigc` 仅展示 `project_id === null`、`tool_asset_role !== null`、`metadata.origin === "aigc"` 的公开资产
- [x] AIGC 来源同时展示画布上传输入和公开生成输出
- [x] “工具资产”排除 AIGC 工作台资产
- [x] “全部资产”包含三类来源且同一资产不重复
- [x] 同时具有 `project_id` 和 `origin=aigc` 的异常历史资产稳定归入项目资产
- [x] 项目、工具、AIGC 三个来源集合两两互斥
- [x] AIGC 来源下项目筛选不可用且不提交 `project_id`
- [x] 未知 `source` 查询参数回退到默认来源行为
- [x] 状态、资产分区和关键词可与 AIGC 来源正确组合
- [x] AIGC 来源无匹配资产时显示专属空态
- [x] AIGC 输入卡片标明“AIGC工作台 / AIGC 输入”
- [x] AIGC 输出卡片标明“AIGC工作台 / AIGC 输出”
- [x] AIGC 资产预览详情显示正确的来源与输入/输出角色
- [x] 内部底图和拆分图层不会出现在普通资产库
- [x] 新生成公开输出名称符合 `画布名-节点显示名-资产类型序号.扩展名`
- [x] 新生成公开输出写入 `metadata.name_scheme="aigc_canvas_node_v1"`
- [x] 自定义节点标题优先，默认节点名与画布显示规则一致
- [x] 多输出资产按任务 ordinal 使用稳定的 1-based 序号
- [x] 非法字符替换、分隔符折叠、固定空值回退和 180-byte 长度边界得到安全处理
- [x] 超长名称先截断画布名、再截断节点名，并完整保留类型序号和扩展名
- [x] PNG、JPEG、WebP、MP4、MOV 的类型文案与扩展名符合固定映射
- [x] AIGC 上传输入保留用户原始文件名
- [x] 画布或节点改名不追溯修改历史资产名称
- [x] 历史 AIGC 资产无需回填并继续按既有规则展示
- [x] 资产卡片、关键词搜索和默认下载名使用新名称
- [x] 新命名资产优先使用 `metadata.name`，无方案标记的历史资产保持既有显示优先级
- [x] 命名或 MIME 映射失败不会留下孤儿对象或资产记录
- [x] 新命名不会丢失 pipeline、run、node、task 和输入输出引用元数据
- [x] 对象存储 key、资产 ID、引用保护和缓存语义保持不变
- [x] 后端相关测试和完整 pytest 通过
- [x] 前端完整 Vitest、TypeScript、ESLint 和 production build 通过
- [x] Playwright 在 1440x1000、1024x768、390x844 视口通过
- [x] Playwright 无控制台错误、横向溢出、控件重叠或媒体比例回归
